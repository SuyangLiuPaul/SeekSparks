# HANDOFF

Running log of what each autonomous iteration shipped. Newest entry on
top. One line per ship — describe what changed and why, not how.

The launchd prompt that drives these iterations expects this file to
exist and to be appended to as the last step of every run. It was
deleted by mistake in `be8176f` (the YsWords-remnant audit, 2026-07-23)
and silently missing until 2026-08-06. Recreated then with everything
back to v1.5.0 reconstructed from `git log`, plus a single new entry
for that day's ship. From here on, each run appends one line.

---

- **v1.6.51** — **the gazetteer that shipped unread for months now answers "which places does this passage name", and the map is a lens over the centre pane (task #277).** `assets/bible_places.json` was in the repo, referenced by nothing — the first job was to establish what the feature *is*, and **bwh33 says something the ticket did not.** BibleWorks' map module is a full cartography suite (raster basemaps, stacked editable overlays, a polyline editor, a ruler with travel-time estimates) and this app will never compete with it; its ties to the *text* are exactly three, and **all three are manual, English-only, and leave the passage**: right-click an English word → "Lookup in BibleWorks Maps" (which requires the reader to already know the word is a place, i.e. the very knowledge a gazetteer supplies); right-click a site on the map → search the text; and **66 pre-made per-book site maps opened by hand from File | Open**. That third one is the closest match and its granularity is the **book** — Genesis' map is a map of the ancient world. **So the gap is not cartography, it is that nothing follows the verse you are on**; I corrected my own source comment mid-run once the help file showed the 66-map fact, rather than leave a claim that overstated the gap. **The base map's provenance was verified, not trusted.** `assets/bible_geo.json` was inherited from an earlier iteration killed by a usage limit, with no generator script — its "Natural Earth 50m" claim is exactly what a fabricated file would also claim, and a file of the wrong provenance here is a licensing incident, not a bug. Downloaded the three upstream layers and compared vertices at 3 dp: **coast 400/400, lakes 400/400, rivers 350/350 exact; land 3209/3220, with all 11 exceptions lying precisely on the crop box `[-15, 5, 82, 50]`** — i.e. crop corners, which is what cropping *does*. Public domain, correctly attributed, demonstrably not BibleWorks raster data. **The data needed repair before it could be indexed:** a global case-replace of the substring `eph` had run over the gazetteer at some point and damaged **eight** three-letter book codes carrying **69 references** between them, every one of which would otherwise be dropped or filed under the wrong book (`nah`→`nAlefh` and friends; the table is in the research notes). **The ticket's tab-strip worry was real but misdiagnosed.** It assumed an 11th tab kills labels and suggested grouping; reading `analysis_tabs.dart` showed the strip already wraps into balanced rows and the actual bug was that `showLabels` divided the available width by **all** items rather than by the items **on a row** — so a wrapping strip measured itself as though it did not wrap, and had been doing so since the strip started wrapping four tabs ago. Fixed by extracting the whole rule into a pure `analysisStripLayout(width, count)` returning `(perRow, showLabels)`, which is testable with no widget tree; the 12th tab therefore **cost nothing and gave the 11th its labels back**. No grouping needed. **The map is a transient lens, not a `WbCentreMode.map`** — centre modes are *persisted*, so a map mode would let the app reopen tomorrow onto a coastline instead of onto scripture, and it would have needed ~8 touch points against the lens's 3. It inherits v1.6.50's rule directly: the status bar names the mode **you can see**, so it reads "Map" while the lens is up and tapping it puts the lens down; and `_setCentreMode` now closes the lens **before** its unchanged-mode early return, or picking "Reader" while the map is up would appear to do nothing. **The second tier is what makes the tab worth opening:** most verses name no place at all, so a verse-only pane would be empty most of the time and read as broken — hence "Named in this verse" plus "Elsewhere in this chapter", with the verse's own places lifted out of the chapter list so nothing is listed twice. **Two bugs were found by writing the tests, not by using the app:** the scale bar was **invisible on first open**, because `_footer` and the map's `LayoutBuilder` are siblings in a Column and the footer builds first, so `_proj` was still null when the footer rendered and nothing rebuilt it afterwards (fixed with a post-frame `setState` on first fit, pinned by a test that opens the map and asserts a `km` readout **without touching anything**); and the dead-`_setCentreMode` case above. **Verification:** analyze exits **0**, **1637/1637 green** (+27, all against the REAL bundled gazetteer — the whole point of #277 is that the file shipped unloaded, so a fixture would test the wrong thing), and **screenshot-verified live on seeksparks-dev at v1.6.51**: the Analysis strip renders **two labelled rows of six** at a 426 px pane (the fix, visible); the Places tab on Jonah 1:2 lists **Nineveh** under "Named in this verse" and **Joppa**/**Tarshish** under "Elsewhere in this chapter" with the Eagle's View credit rendered beneath; tapping Joppa opens the lens over the centre pane with a **500 km** scale bar and the ruler reading **"Joppa → Nineveh 908 km · about 29 days on foot"** and **"Joppa → Tarshish 3778 km · about 119 days on foot"** (both within a percent of hand-computed haversine), the status field reads **"Map"**, and "Close map" returns to the reader with the pane intact; at a 356 px pane the strip degrades to **icon-only two rows of six** with no overflow. **Only assumed, not verified:** the Chinese surface — 约帕/本节地名 are pinned by widget test but were never screenshotted live. **Judgement call left open:** `_kMinLabelledTabWidth = 66` still ellipsises seven of the twelve labels ("Wor…", "Cont…", "Plac…"); that is pre-existing behaviour at 11 tabs, not a regression, and raising it would cost the labels entirely — left alone deliberately. **Traps for the next run:** CDP `Emulation.setDeviceMetricsOverride` is silently **dropped after `Input.dispatchMouseEvent`** on this build — `window.innerWidth` keeps reporting the emulated value while the page reflows to the real window and `Page.captureScreenshot` returns a 2× image, so every post-click screenshot lies about the layout it is showing; **resize the real window with `Browser.setWindowBounds` via the `/json/version` browser endpoint instead** (`/tmp/ss_win.js`) and take screenshots at native dpr 2, downscaling for reading. AppleScript window control times out (no automation permission). And a commit message containing an apostrophe inside a `$(cat <<'EOF')` heredoc breaks bash parsing — write the message to a file and use `git commit -F`.
- **v1.6.50** — **split view is a workbench MODE now, so two columns stop costing you the workbench (task #276).** The item was filed as a *styling* problem — “the reader is `Get.off(() => const HomePage())` at workbench_page.dart:299 and :854; render it in the centre pane, then restyle it to workbench_theme.dart:16 (square corners, hairlines, no shadows)”. **Read the code before believing the ticket:** line 832-835 was already `parallelMode && threePane ? _buildParallelFrame : BibleReadingPane`, so the centre pane was *already* the reader, already in workbench chrome — the radius-20/BoxShadow/floating-pill styling belongs to `HomePage`, a different route, and restyling it would have changed nothing anyone sees. What those two buttons actually did was **leave the route**, taking the command line, the search results and the Analysis pane with them; a reader who wanted two columns paid for them with everything else on screen, unwarned. **Two columns was the only capability HomePage had that the workbench lacked**, so it moves in: `WorkbenchProvider.parallelMode` (bool) becomes `centreMode` (`reader | browse | split`). **Three modes, not two, because BibleWorks keeps three and bwh11 says why** — “there is not always a one-to-one mapping between verses in different versions” (Hebrew psalm superscriptions carry a verse number English editions fold into a heading; the LXX numbers Jeremiah differently). Interleaving editions under one verse number is a quiet lie whenever they disagree; bwh38’s Parallel Versions Window uses **columns** so the reader’s eye does the aligning. **Split is not a narrower Browse — it is the mode for the case Browse cannot represent honestly.** The bool was **deleted, not kept as a derived getter**: with three modes `!parallelMode` no longer means “the chapter reader”, so every site still reading it that way would have been wrong *in split mode without failing to compile* — three compile errors beat one silent behavioural bug. **`kMinCentreWidthForSplit` is derived, not chosen**: `ResponsiveBreakpoints.minReadingPaneWidth * 2 + 16` = **976**, and a test asserts that arithmetic, because the split columns are literally the same `BibleReadingPane` at the same type size — inventing a smaller floor would assert a reading column is narrower inside the workbench than outside it. It measures the **centre pane, not the window** (the rails collapse to 44px), so “does not fit” is about the current *arrangement*; hence split does **not** require three panes, only Browse does — requiring it would make the control unreachable at exactly the moment collapsing a rail makes it usable. Consequently the View menu **greys Split with the reason “needs a wider centre”** (teaching the reader what to close) while the toolbar button **hides** (no room to explain itself) — the v1.6.17 lesson that offering a benefit and then withholding it is the failure mode. **Latent bug fixed in passing:** HomePage seeded the second column with `firstWhere(v.book == primary.currentBook, orElse: () => verses.first)`, and since v1.6.47 `Verse.book` carries the *edition’s* book name while `defaultSecondaryVersion` deliberately picks a different edition — often a different language — so the match failed and the second column **silently snapped to Genesis 1**, which is the *normal* case, not an edge one. Now matched through `bookNameToEnglish` on both sides with no `orElse`: no match leaves the column where it is. **Grep `firstWhere(..., orElse: () => list.first)` across two versions next run — every one is this bug.** The #274 warm-up learned the third mode too (split warms only its second column, not the three-edition stack) and shares `resolveSecondaryVersion` with the pane, or it would warm one Bible while the pane fetched another. `home_page.dart` **stays** — now capability-free but still the target of a dozen `pushPage` calls and the boot route; retiring it is a product decision about whether the app has one reading surface or two, recorded in its own header doc rather than folded into this slice. **The screenshot caught what 1574 green tests could not:** v1.6.49 shipped with the status bar reading **“Split” while a single fallback column was on screen** — the field named the stored preference, not `effectiveCentreMode`. The comment directly above it *claimed* the label could never lie, which was true of the tap *cycle* and irrelevant to a *persisted* preference meeting a narrower window, i.e. the only path a real reader takes. **A comment asserting an invariant is not the invariant, and it made the gap harder to see.** Chasing it found a second bug: `_nextCentreMode` called `order.indexOf(_wb.centreMode)` on a list that had already had non-fitting modes removed, so `indexOf` returned **-1**, `(-1+1) % len == 0`, and tapping restarted the cycle from the top instead of stepping off what was visible. Both fixed in v1.6.50; pinned by a **property** test rather than examples — for every mode × {0, 480, 975, 976, 1400} × {threePane, not}, `effectiveCentreMode` must be a **fixed point of itself**, which is the machine-checkable form of “the status bar can only name something the layout will actually draw”. **Verification:** analyze exits **0**, **1575/1575 green** (+25), and **screenshot-verified live on seeksparks-dev**: split renders two columns *inside* the workbench with the command line intact; `Joh 3` typed at the command line moved **both** columns; with the left column switched to CUVS the headers read **约翰福音 3** beside **John 3** with no snap to Genesis, which is the cross-language case that used to fail; at 1024 the View menu greys Split (centre = 668 < 976) and the status bar now reads **“Reader”** while `localStorage['flutter.workbench.centreMode.v1']` is still `"split"`; widening to 1440 brings both columns back without re-picking the mode. **Traps for the next run:** the columns icon in the centre-pane header is the *version-stack picker*, not split — the split control is in the **View menu**, and at 1440 with both rails open it is legitimately greyed, so a screenshot run must close a rail first; and the CDP driver needs `Input.insertText` + `Input.dispatchKeyEvent` steps to drive the command line (added to `/tmp/ss_shot.js`).
- **v1.6.48** — **the workbench’s first paint was gated on downloading Bibles it already had (task #274).** The item was filed as "panes render empty until an interaction forces a repaint… trailhead BibleReadingPane / `wb.analysisVerses`", and **both halves are wrong** — no interaction is needed (the content arrives on its own; the reporter simply clicked first), and the pane that greets a new reader is the *Browse stack*, because `workbench_page.dart:832-835` is `parallelMode && threePane ? _buildParallelFrame : BibleReadingPane` and `workbench.browseMode.v2` defaults to **true**. BibleReadingPane renders from `MainProvider.verses`, already resident; it was never the slow one. **The real shape, measured by CDP rather than inferred:** CPU is innocent (58 fps, no frame gap >150 ms, longest task 89 ms) and so is the boot corpus (settled ~1.4 s) — but then the app does *nothing* for ~2.8 s on the splash’s fixed 3 s timer, and only at **4190 ms**, with the pane already on screen and blank, does `BrowseWindow` begin fetching `genesis.json`/`bsb.json`/`kjv.json`/`hebrew.json`. **A symptom described as *rendering* was really *scheduling*** — which is exactly why 1427 tests passed with it live: a test awaits its futures, so it never experiences "not yet". **The architectural cause:** `BrowseWindow` and `ParallelVerseView` each kept a private `static Map<String, List<Verse>> _versionCache`, separate copies alongside `MainProvider`’s real LRU — so the reading version already in memory was re-downloaded and re-parsed in order to display it, and neither private map could be reclaimed by `dropCachesOnMemoryPressure()`. Both now read the one shared LRU; `preloadVersion` gained in-flight dedup (three call sites can now race for the same edition) and **lost** a guard that refused to load `currentVersion`, which was wrong precisely when boot had failed. **The fix** (`lib/services/workbench_warmup.dart`) spends the splash’s idle 3 s on the next screen — and the restraint is the point: it warms **only what the pane about to be shown will itself demand**, never speculatively, so `browseWarmupOrder` returns **empty** when `parallelMode == false`, honouring the v1.3.61 decision not to eager-preload over mobile data. The 3 s splash was deliberately left alone; shortening it is a product call belonging to #281. The planner is a pure function, so the *decision* is testable with no assets and no SharedPreferences — copy that pattern. Pref keys and the per-locale default stack moved into the service and `WorkbenchPage` now reads them from there, because if the two drift the warm-up loads editions the page never shows and the pane still starts cold; a test pins the literal key strings, since renaming either silently resets every reader’s Browse stack. **Verification:** analyze exits **0**, **1552/1552 green** (+11), and **screenshot-verified on seeksparks-dev at v1.6.48** — warm cache, saved stack bsb/nasb/kjv, reading NASB: `bsb.json` at **1559 ms** and `kjv.json` at **2025 ms** (was 4190 ms), `nasb.json` **not re-fetched at all**, with originals, Strong’s and tagged text all banked by ~3.4 s while still on the splash. The first workbench frame shows the new labelled state reading **"Preparing this chapter"** — *not* "Loading editions · BSB · KJV" — and that distinction is itself the proof, since the label names only editions still missing from the cache; a second run painted fully populated with no loading state at all. A profile with `browseMode.v2` set to false fetched **no** `bsb.json`/`kjv.json` whatsoever, confirming the conservative path live. **Not measured, and the remaining thread:** the exact duration of that "Preparing this chapter" state (per-row chapter assembly in `BrowseWindow._load()`, CPU not network) — under ~3 s in one run, absent in another. **Traps for the next run:** clicking the Browse/Reader toggle to set up a measurement is unreliable because splash length varies from 2.9 s to over 9.5 s on the same build, so a fixed wait lands on the splash and the click is swallowed — set the pref in localStorage before navigating instead (SharedPreferences on web is localStorage with a `flutter.` prefix, bools stored as the strings true/false); `Network.setCacheDisabled` re-downloads `main.dart.js` and shifts the whole timeline seconds to the right, so use the warm cache for regression comparison; ink-coverage of a screenshot cannot by itself distinguish the splash from a populated pane (both ~5 %), but the splash is *static* while content jitters ±0.3 % frame to frame; and `flutter analyze | tail` reports **tail’s** exit code, always 0.
- **v1.6.47** — **the book name must follow the reading version, not the UI locale (task #283).** The report was a Chinese UI on CUVS(简) whose KWIC list printed `Numbers 32:18`, `Deuteronomy …` ellipsised. **The cause was a missing INPUT, not a missing call.** Book names have TWO renderers — `localeAwareBookName` (formal) and `shortBookName` (3-letter abbreviation) — and only the formal one had ever been given the version, so the abbreviation followed the UI. That produced the symmetric defect nobody reported: a Chinese UI reading BSB got `民` printed beside a verse that reads "Numbers". Both now go through one rule, `bookScriptFor(locale, version)` in `book_name_mapping.dart`: a version, if present, decides (`kjv/leb/nasb/bsb/kjvs/lxxwh` → English, `-tr` → Traditional, else Simplified) and the locale is only the fallback for surfaces holding no version. **The trap that would have flipped the app:** `toLocale('Numbers','')` has always returned `民数记`, not `Numbers`, because several callers pass `mp.currentVersion` before it loads; expressing it as `bookScriptFor('', v)` quietly made an unloaded version mean English and would have changed every book name during startup — it is pinned by a test now. **Why the KWIC column takes the ABBREVIATION and the clipboard does not:** localising alone would not have fixed the reported truncation, since `帖撒罗尼迦前书 5:23` is *wider* in the 84 px column than the English it replaced; BibleWorks bwh42 documents that it abbreviates its own reference columns for exactly this reason, while a pasted reference leaves the app and keeps the formal name. The English-keyed corpora are the ones at risk — `concordance.json` is keyed `Numbers 32:18` and `ConcordanceRef.label` must STAY canonical English because `tryParse` reads it back for navigation, so the new `ConcordanceRef.display(locale, version, {short})` renders without touching the data. **The audit's own near miss, worth inheriting:** a subagent sweep flagged ~20 sites of the form `'${v.book} ${v.chapter}'` as bugs and missed `kwic_pane.dart` — the exact surface in the screenshot. `Verse.book` arrives from the version file ALREADY in that version's script (`book_name_mapping.dart:247` says why: the reading pane filters by book NAME), so the mass edit would have double-translated every one of them. Rule: **check whether a string is English-keyed data or version-loaded text before localising it.** Fixed for real: kwic_pane (list + clipboard), word_forms_section, and `version` threaded into word_analysis/morph_search/related_verses/phrase_match panes plus copy_center, dashboard, the reading pane's floating header and bible_trivia. **Deliberately NOT changed:** `stats_page.dart:2290` prints a raw English reference only as the fallback for a missing `ui_strings` key — localising it would hide the missing key. **Verification:** analyze exits **0**, **1541/1541 green**, 21 new tests in `test/book_script_test.dart` including "no Latin letters survive under a Chinese version" and a corpus test that all 66 books abbreviate to ≤3 characters in all three scripts (which is what keeps the last-character fallback unreachable). **And the browser screenshot path from v1.6.44/46 is UNBLOCKED** — a ~90-line CDP driver on Node 24's global `WebSocket` (no installs) drives the deployed dev build; two traps: filter `/json/list` by URL because Chrome's update tab steals index 0, and sleep ~1.8 s before `Page.captureScreenshot` or you photograph a stale Flutter frame. **Screenshot-verified on seeksparks-dev at v1.6.47, both directions, same word G3956 and same tab:** CUV+S(简) → `太 1:17 / 太 2:3 / 太 2:16`, BSB → `Mat 1:17 / Mat 2:3 / Mat 2:4`. **Observed but not acted on:** the workbench centre pane still shows a spinner for several seconds on first paint — that is queue item #274, untouched.
- **v1.6.46** — **the divine name was being printed as a translator's insertion (task #282).** The report was cosmetic — `主 [雅伟]` on some KWIC lines, `主 [ 雅伟]` with a stray gap on others — and the spacing turned out to be the least of it. **Start with what the bracket MEANS, counted rather than guessed:** every square bracket in `cuvs-yhwh.json` is one of exactly two tokens, `[雅伟]` × 212 and `[基督]` × 17 (`cuvs-yhwh-tr` mirrors it with `[雅偉]`), so the convention is closed and enumerable. Reading the contexts settles what it is: `X[雅伟]` is a **referent gloss** — the word X printed here refers to Yahweh — marking κύριος where an OT quotation had YHWH, and in a handful of OT verses marking a PRONOUN (`我[雅伟]`, `他[雅伟]`) whose antecedent is the Name. `[基督]` is the identical device pointing at the Messiah, and **Matthew 22:44 carries both four characters apart**, which is why they cannot be told apart by styling one as an insertion. **The direction is the whole point:** a supplied word means the target has MORE than the source (`darkness [was] over`), a gloss means the source had something the target LOST. `scripture_markup.dart` had ONE bracket rule — `_suppliedRe` → `supplied` → italic — so the app set 雅伟 in the convention that has meant "the translator added this" since the Geneva Bible, i.e. the reverse of the claim the edition exists to make. **Three consequences, one root.** (1) Semantic, as above. (2) **The clipboard, the one with effects outside the app:** `copyVerseText` resolved the bracket under `keepSuppliedBrackets`, an option that asks "mark the translator's insertions?" — so a reader answering *no* pasted Matthew 22:44 as `主雅伟对我主基督说`, **a divine title Matthew never wrote**, with nothing on screen to show it was never there. Those brackets are now unconditional. The same flattening lived in **five copy-pasted result-list preview pipelines** (search_page ×2, command_pane, originals_sheet, strongs_entry_page) — now one `versePreviewText`, which is precisely why all five carried the same bug. (3) **The tagged assets, and the worst of the three:** `tools/import_yahweh_modules.py` walked the text word by word and split the gloss across runs in **193 verses** — `{"w":"有主 [","s":"G2962"}, {"w":"雅伟] 的使者","s":"G32"}` — so the closing half was TAGGED, and **hovering the divine name in Matthew 2:13 answered "angel"** (G32 = ἄγγελος), a wrong lexicon answer nobody had reported. KWIC joins runs with a space, so the keyword column for G2962 held an orphan `主 [` while `雅伟]` opened the right context: **that** is the `主 [ 雅伟]` in the screenshot, and the un-split cases (the importer also spaced the bracket off from the Chinese, which the base JSON does not) explain the other form. **One cause, both symptoms.** `TaggedTextService.reuniteGlossRuns` puts the gloss back on the run that opened it, once at load, so KWIC / Browse / hover all get repaired runs without knowing. **The near miss worth inheriting: bracket SHAPE is not a safe key.** Measured across all six tagged sets — bsb 18,744 bracketed runs 0 split, cuvs-yhwh 193 splits, lxxwh 184, nsn-plus 42 — and the LXX/WH and NASB splits are **legitimate**: `[το αυτο]`, `[ And Philip said … of God."]` are textual-critical brackets around disputed *words and whole verses* where every word inside is a real word with a real number. Reuniting those would be vandalism, so the repair is keyed to the two tokens and a test asserts bsb/kjvs/lxxwh/cuvs-plus/nsn-plus come out unchanged. Transformation was prototyped in Python against the real assets before any Dart: 198 verses changed, zero runs dropped. **A test earned its keep by being wrong:** the first asset assertion was "the run carrying `[雅伟]` must be tagged G2962" and the data refuted it in twelve places — δεσπότης G1203 at Jude 1:4 / Luke 2:29 / Rev 6:10, θεός G2316 at Acts 16:32 where the manuscripts read θεοῦ, κατέναντι G2713 at Romans 4:17 where a preposition phrase swallowed the 主 — all correct, because **the gloss attaches to whatever word precedes it, not to κύριος**. The shipped invariant is structural instead: *no run may OPEN with a gloss*, because then its number belongs to a word printed after it. **Rendering:** brackets kept, body upright at full text colour, brackets muted — kept because unbracketed `主雅伟` misquotes the verse and weight alone cannot carry that boundary at workbench sizes in CJK; upright because italic is the insertion convention; and the two kinds print **identically** because the edition sets them identically and giving the Name extra weight *here* would make the 212 glossed occurrences louder than the thousands where 雅伟 simply stands in the text unbracketed. **Deliberately NOT done, and both are judgement calls a human may overturn:** `主*` (Matthew 9:28, Luke 24:34, plus traditional mirrors) is left verbatim — both are κύριος addressed to Jesus so it is *probably* `主[基督]` with the gloss dropped, but that is an inference from two data points with no edition key available, and inventing a rendering for an unknown editorial mark is the class of guess this project must not make about scripture; note the brief cited Matthew 7:22 / 8:2 / 8:6 for this form and **those three verses contain no asterisk in any bundled version**. And the **classic reader was already innocent** — `build_verse_content_spans.dart:285` has since 2026-06-30 rendered the body in the theme accent with a thin underline, notation rather than insertion — so it was left alone; it drops the brackets the workbench now keeps, a one-character-pair divergence that is a product decision, not a defect. **Verification, honestly separated:** analyze exits **0**, **1520/1520 green**, the new suite has three asset-level tests that run against the shipped JSON (the bug survived 1,475 green tests because every one used strings a human wrote and the defect was in a file an importer wrote), and a "premise" test asserts the raw asset really does still split the gloss so the repair cannot quietly become a no-op. Deployed from a clean worktree at the pushed SHA and **string-probed on the immutable deploy URL**: `YHVH`, a token introduced by this commit and present nowhere at `70bd2ae`, appears exactly once in the served `main.dart.js`. **NOT verified: no browser screenshot.** The headless-Chrome path is still blocked by the loading page (see v1.6.44); what stands in for it is a widget test that pumps the real render path and asserts the painted plain text is `有主[雅伟]的使者` with no italic span and muted brackets — a render-contract check, not a picture.

- **v1.6.44** — **the iPad command-line wipe (task #273), fixed as a defect class rather than as three lines.** Tapping an operator chip on iPad put the token in, and then the reader's *next keystroke emptied the field*. **The name of the bug hides the mechanism: `controller.text = x` is not a plain setter.** Flutter implements it as `value = value.copyWith(text: …, selection: TextSelection.collapsed(offset: -1), composing: TextRange.empty)` — so the two-statement idiom (`.text =` then `.selection =`) publishes **two** `TextEditingValue`s, and **the first one carries a selection that describes no position in the text**. On desktop and web the second lands before anything observes the first, which is exactly why this was invisible here and why **1400+ green tests said nothing**. iOS drives text input *asynchronously*: the engine can latch the caret-less state and the platform's echo can arrive *after* the framework has already set the real caret, leaving the field holding a selection that does not describe its text — and the next keystroke is applied against that range, which is why the symptom is "everything disappears" rather than "the caret is in the wrong place". One assignment leaves no window for the two to disagree. **`lib/utils/atomic_text_edit.dart` → `setTextAtomic(text, {caret})`, with two deliberate properties, not incidental ones:** the caret is **clamped into range**, because *an out-of-range selection being unrepresentable* is the invariant the helper exists to hold; and `composing` is **cleared**, because a composing range is measured against the *old* text and can point past the end of the replacement — which matters more here than in a generic app, since `bwh17b_CommandLineFarEast.htm` shows BibleWorks built its command line an entire dedicated three-mode IME (toggled with `<insert>`, a coloured flashing box showing the mode), i.e. **a Bible workbench's command line is an IME surface as a matter of course**, and this app's readers type Chinese into it. **The brief named three sites; the class had eight.** Five more were reachable on iPad and none were in the report: the **recent-search tap on the search page** (the same text-then-selection race), and four that assign `.text` with *no second statement at all* and so simply publish `-1` and leave it — the **settings paste-key field**, the **Gemini key card** (which can fire *while the field is focused*, from a cloud sync), the **feedback prefill**, and the **`+Verse` fallback branch** in the reading pane, whose *other* branch was already atomic. **Verification, honestly separated:** analyze exits **0** (checked by exit code — the first run failed on an info-level `unnecessary_import` that a grep for "error" would have shipped), **1475/1475 green**, and the new tests are proven to *bite*: reverting `_insertToken` to the old two-step fails them with `Expected: <1> Actual: <2>`. The mechanical grip is to **attach a listener and assert on the published value sequence** — exactly one publication per interaction, never a `baseOffset < 0` — since asserting on the final text alone cannot see this bug at all. A "premise" test pins the Flutter behaviour the fix depends on (`c.text = x` really does leave `selection.baseOffset == -1`), so if that ever changes, the file says so instead of quietly becoming cargo cult. **NOT verified, and this must not be read as fixed on device: there is no iPad here, and the symptom cannot be reproduced on any other platform by construction** — what is confirmed is that the cause is gone. **Dead end worth inheriting:** `debugDefaultTargetPlatformOverride = TargetPlatform.iOS` does *not* help, because `flutter_test`'s fake text input applies editing state synchronously, so the race that *defines* the bug cannot exist in the harness. **Also not verified:** headless Chrome renders the deployed v1.6.44 splash but never reaches the workbench — the loading page arms its auto-advance only once verse data arrives, and `--virtual-time-budget` starves those fetches — so the dev deploy is confirmed live and running Dart, but no rendered workbench screenshot was obtained.
- **v1.6.43** — **the Context tab: what this passage is made of.** BibleWorks `bwh10h` lists the words of the pericope, chapter and book around the focused verse, "to identify likely themes". **A naive reading — "a word list with a scope selector" — misses the three things that make it worth building, and a published review names the failure directly: in BibleWorks itself you "have to scroll past insignificant high frequency words like articles, pronouns, and prepositions".** (a) **The unit is the PERICOPE**, and `assets/section_titles.json` had only ever been read as a heading to print above a verse — `lib/utils/pericope.dart` reads it as *ranges*, which is the one scope the already-shipped Word List Manager (`bwh26`) structurally cannot express, and which is why this is not that feature again. A pericope crosses chapter boundaries (so the caller must supply chapter lengths) and a verse can precede the book's first heading; both are handled, not assumed away. (b) **Function words are filtered by default**, which requires a part of speech — and `assets/originals/` carries **two incompatible tagging schemes**, Greek CCAT (`N-----DSF-`) and Hebrew/Aramaic OSHB (`HR/Ncfsa`), **in which `R` and `P` mean OPPOSITE things** (Greek `P` = preposition, Semitic `P` = pronoun). `lib/utils/word_pos.dart` picks the scheme off the Strong's *prefix*, never the code, and resolves the Semitic head morpheme, which sits at **neither a fixed start nor a fixed end**: `HTd/Ncmpa` (Hebrew article prefix) heads *second*, `ANcmsd/Td` (Aramaic determined state) heads *first*, and `HR/Ncmsc/Sp3ms` heads in the middle — so "take the first segment" and "take the last segment" are both wrong and both pass a hand-written test. The bare adjective tag `Aamsa` must **not** be read as Aramaic, so the language marker is stripped only when char[1] is uppercase. (c) **The default ordering is keyness (log-likelihood G²), not raw count**, measured against **the containing book MINUS the scope** rather than the whole Bible — authorial vocabulary varies so much between books that scoring John 1 against Leviticus mostly rediscovers that John wrote John, and excluding the scope from its own baseline stops a locally-exclusive word inflating its own expected value. **The payoff, asserted end-to-end against the real assets rather than mocks:** John 1:1–18 goes from a list headed `ὁ ×34, καί ×16, αὐτοῦ ×15` to one headed `χάρις, γίνομαι, φῶς, θεός, μονογενής, σκηνόω` — the prologue's own vocabulary — and Genesis 1 to `אֱלֹהִים, רָקִיעַ, מִין, אוֹר, בדל`. **The eleventh tab exposed a real layout defect and the test suite caught it:** eleven bare icons no longer fit across a 320 px Analysis pane and `AnalysisTabStrip` answered by *clipping* them; it now wraps to a **balanced** second row (6+5, short row padded so every tab keeps the same width) rather than scrolling, because a tab hidden behind a scroll affordance on a 320 px pane is a tab nobody finds — and the selected tab is now the *last* one, which is exactly the case scrolling would have hidden. The two strip tests that hard-coded `width: 700` now **derive** the threshold from `AnalysisTab.values.length`, so the next tab does not break them. **Conservative calls, all reported:** whole-Bible keyness was **not** attempted — it needs either 66 book loads or licensed NT-only statistics — so the book scope honestly reports *no baseline* and shows no keyness at all rather than a number that means nothing; `sectionTitleSetByVersion` was **not** extended to cover `bsb`/`kjvs`/`lxxwh`/`cuvs-plus` (that would change reading-pane headings for five versions, well outside this feature), so the Context tab uses a **local** locale fallback, which is safe only because all three heading sets were verified to sit on identical verse boundaries (1443 headings each, same keys) — the choice changes the title text, never the range. **Known limitation, not a bug:** on a very small scope against a large baseline (Psalm 23) keyness degenerates toward pure rarity, which is the statistic behaving correctly. **Verification, honestly stated:** analyze clean and **1466/1466 green**, including two widget tests that render the *real* `ContextPane` against the *real* asset bundle at both ends of the Analysis pane's width (320 and 560 px) and assert no overflow, the pericope label `1:1-18 · The Word Became Flesh`, the counts `18 verses · 252 words · 83 distinct`, the presence of χάρις and μονογενής **and the absence of ὁ** — the whole thesis of the feature, checked on real data. **A real browser drive was NOT done: `flutter devices` exposes no Chrome/desktop device in this unattended launchd session and there is no click-automation binary, so "looks right when clicked" is asserted from rendered-widget tests, not observed.** There is also **no golden/font infrastructure** in this repo and no bundled Greek or Hebrew font, so a pixel golden would have rendered the very text that matters as boxes — which is why the check above asserts on rendered *text*, not pixels. **Ship bookkeeping, and the rule kept this time:** the tree was shared — v1.6.42 plus two Eagle's View data commits landed under this run — so paths were staged **by name, never `git add -A`**, every diff was checked for deletions that would have reverted the other writer, and the dev build ran in a **detached worktree** at the pushed SHA (`7e58aab`), with the version bump then re-applied to the shared tree **by editing, never by `cp`**. **Pre-existing data defect noticed, deliberately not fixed:** the Strong's gloss for `φῶς` renders as "compare G5316 (φαίνω), G5346 (φημί))" — a truncated lexicon entry in the bundled data, visible in any pane that prints glosses, not caused by this change.

- **v1.6.40** — **the Copy Center: what leaves the app, and under whose licence.** BibleWorks `bwh28` (Copying Results) + `bwh29` (Output Format Options). **A naive reading of "add a copy button" misses four things, and all four are why this is a dialog:** (a) a reference list with **no text** is a *separate artifact* — what a footnote or index is made of — not a degenerate case of copying text, and `bwh29` gives it its own separator set; (b) merging consecutive verses is real logic, not string joining — `Gen 1:1–2; 2:1` has to know where a chapter and a book end; (c) the separators are **configurable because the world is not the USA** (`Gen 1,1` is how a German reader writes it, so hard-coding `:` is a parochialism, not a default); (d) multi-version copy has **two shapes** — grouped by verse, or by version — and neither is a superset of the other. **Two things BibleWorks does not need but we do.** The apparatus (`[supplied]` words, `<note:>` footnotes) has to be a *choice*: brackets are damage in a handout and a scholarly claim in a paper. And copying puts publisher text on someone else's page, which is **the** moment the licence has to travel with it — so the rights line is reused **verbatim from the About page** via `attributionKeyFor`, not redrafted at the clipboard, meaning what ships is wording already reviewed, in the reader's own locale. **That obligation is also the feature's ceiling, and this is the part worth keeping:** the Lockman Foundation's published quotation provision for the NASB — the permission the About page *claims* — is **500 verses**, and every other permission held here is narrower ("non-commercial study only"). A Copy Center with no limit is a bulk text exporter with extra steps, so a copy including any licensed edition stops at 500 and the dialog says it was **the licence, not the app**, that stopped it; public-domain editions (`kjv`, `bsb`, and the three Eagle's View rows, whose *text* is public domain even though the electronic edition is not) are uncapped. **Architecture: a pure core** — `lib/utils/copy_format.dart`, no Flutter and no IO, with book names, verse text and rights all arriving as injected callbacks, so the whole engine is testable without a widget or an asset (**44 tests**, including `bwh29`'s own worked example verbatim). `ffThreshold` defaults to **0 = never abbreviate**: the app knows the exact verse list, `1:1-4` is unambiguous and `1:1ff` is not, and both SBL and Chicago discourage it — the setting exists because `bwh29` has it, the default disagrees with BibleWorks on purpose. **Entry points:** File ▸ Copy…, a toolbar button, and **Ctrl/Cmd+Shift+C — Shift deliberately**, because plain Ctrl+C is the browser's own copy and is the right tool for a selection the reader already made with the mouse; taking that key would break it page-wide for one dialog. **The narrow-layout widget test caught a real crash** the desktop test could not: `_options()` returned a `ListView` that the ≤900 px branch nested inside another `ListView`, i.e. an unbounded-height viewport — now a `Column`, scrolled by its parent. **Deliberately NOT touched:** the mobile reader's three-mode quick copy (`AppSettings.copyFormat`, devotional/withRef/plain). Those modes were tuned by explicit user feedback ("灵修模式不是一节一行而是全部都一起的") and rewriting them on this engine would change output nobody asked to change — **but that path has no rights line at all today, which is the single highest-value follow-up here.** Conservative calls, all reported: plain text only (no `text/html` clipboard flavour — it needs JS interop around the crash-hardened `ClipboardHelper`, which exists precisely because iOS Safari rejects the async API); curly `“ ”` hard-coded (wrong for zh-Hant, which wants 「」 — a real imperfection, smaller than a quote-character setting nobody would find). **Ship bookkeeping — I broke my own rule and it is worth recording:** `tools/release_web.sh` was run **in the shared working tree**, and the other writer's `assets/ot_synopsis.json` was written to disk *mid-build* (18:49, ~2 min after the build began) and was swept into the bundle, so the v1.6.40 dev deploy is **a mix of my `2b4b02e` and their in-flight OT-synopsis work**. Benign in outcome — that asset is reference ranges plus group titles, no verse text, committed by the owner in `00bce47` carrying its own "Used by permission" attribution, and they shipped it themselves minutes later — but the lesson stands: build from a detached worktree. They also **swept my v1.6.40 `pubspec.yaml` bump into their `d4e5aa4`**, so there was nothing left for me to commit; the `1.6.41` sitting in the tree at the end of this run is **theirs, in flight — do not commit it.** **Verification, honestly stated:** analyze clean and **1429/1429 green** (44 pure-core + 10 licence/localisation + 7 widget tests that tap through the real dialog at 1280 and 760 px and in zh-Hans); the release web build compiled clean under dart2js; and the deployed bundle was confirmed to contain the feature by string-probing `main.dart.js` on the immutable deploy URL (`Copy Center`, `Sample output`, `publisher quotation limit`, and the Chinese strings as `\u`-escapes). **A real browser drive was NOT done — launching Chrome was denied in this unattended session — so "works when clicked in a browser" is asserted from widget tests, not observed.**
- **v1.6.38** — **the Forms readout: how certain is the parse on screen?** BibleWorks `bwh10q` describes two lookups running in opposite directions, and **the one the name suggests is the less interesting one.** Top section = a paradigm (λόγος → λόγον 130×, λόγος 63×, λόγῳ 45×) — useful, and roughly what you would guess. Bottom section = a claim about **certainty**, and that is the reason to build it: the Analysis pane prints ONE parsing in a box with nothing qualifying it, so a reader had no way to know that the אָדָם in front of them is tagged H120 "man" in 18 places and H121 "Adam" in 2 and that **the tagger picked one**. Guessing this feature from its name would have shipped a list of inflections and missed the point entirely. **The corpus number that drove every decision: 4,756 of 130,950 surface forms are ambiguous — 3.6%.** So (a) the form→parses index stores *only* those 4,756, and a miss decodes to `FormAmbiguity.unambiguous` rather than to an error or an empty state — absence **is** the answer, and 130,950 keys collapse to 319 KB; (b) the ambiguity block renders only when ambiguous, because a badge reading "unambiguous" on 96.4% of words trains the reader to stop seeing the block that matters. **Placement — a section under the parsing box, not an eleventh tab — for a reason worth keeping:** at the Analysis pane's 288 px minimum a `_TabButton` is 17 px icon + 4 px inner + 2 px outer ≈ 29 px, so the existing **ten tabs already come to ~290 px and overflow**; and more decisively, half of `bwh10q` is a *qualifier on the parsing line*, and a qualifier one tab away from the claim it qualifies will not be read. Note for the next reader of the tab strip: `AnalysisTab.morphology` is *labelled* "Forms" but renders `MorphSearchPane` (the GSE) — the label is misapplied and this feature is not that pane. **Sizing was measured, not estimated:** refs-per-triple cost 0→5.62 MB, 1→7.63, 2→8.27, 3→8.61; interning the 66 book slugs to integer indices (`"39 1:1"`) recovered ~1.1 MB; final **7.50 MB over 151 shards, 49.6 KB mean**, sharded by Strong's *hundred* because the caller holds exactly one number at a time — the word under the cursor — and in running text the next word is usually a neighbour, so one small fetch serves many hovers. `count` is the honest total and is never capped; only the three example refs are. **Trap that wasted real time: `slugToBook` maps *abbreviations* (`gen`, `1co`, `3jn`), NOT the full slugs (`1_corinthians`) that name the files under `assets/originals/`** — a crude regex "proved" 18 books were unmapped when none were. Fixed by inverting the canonical names through the same slug rule rather than adding a second 66-name table that could drift, with a test asserting all 66 filenames resolve. **A correction to the research notes, and it is the exact trap the brief warns about:** they claim "bwh36 RWP = Robertson's Word Pictures"; the real `<title>` of `bwh36_RWP.htm` is **"TSK and Nave's"**. The claim was inferred from the *filename*. **Ship bookkeeping, third confirmation of the same lesson:** the shared working tree was red (1 failure, `nasb-ev should be Simplified`) purely from the other writer's uncommitted Eagle's View rows — a detached worktree at the same commit gave **1357/1357 and a clean analyze**; report the worktree numbers. `pubspec.yaml` could not be staged by hunk (the `assets/forms/` addition shares a hunk with the local-only `- assets/tagged/nsn-plus/` line), so the blob was **constructed explicitly** — `git show HEAD:pubspec.yaml`, apply only the intended addition, `git hash-object -w`, `git update-index --cacheinfo` — and then verified with `git show :pubspec.yaml`, i.e. against the *staged blob*, not against a diff. **Two collisions with the concurrent writer, both instructive:** it pushed my commits for me (a `git push` from the shared repo carries whatever is committed there), and it bumped the version while my build was running, so my first dev deploy went out labelled v1.6.37 from a commit that was *not* the v1.6.37 release commit — corrected by re-deploying from the true tip as **v1.6.38**, verified live (`version.json` = 1.6.38, `assets/forms/index.json` = 200, 438,821 tokens). Also **do not `cp` a file into the shared tree without diffing it first**: doing so silently reverted a file the other writer had just committed. It separately discarded the local-only Eagle's View rows from the working tree; they were never committed, and `git show HEAD:lib/constants/bible_versions.dart | grep -c nasb-ev` = 0 at every point in this run.
- **v1.6.34** — **Phrasing: the structure of the sentence made visible, without a canvas.** SeekSparks' answer to the Diagramming Module (bwh25), now reachable from Tools. **Start here: the model, store and page (1,405 lines) were written by the PREVIOUS iteration and left UNTRACKED when its run died on the org usage limit — `run.log` shows eight consecutive `exit 1` iterations after 16:12.** This run finished them rather than starting something new. The lesson generalises and costs nothing to apply: **run `git status` for untracked `lib/` files before picking a backlog item**, because a cut-off run leaves a complete feature on disk that no commit, no HANDOFF line and no research note mentions, and the next run's instinct is to start a fresh item beside it. **Why an indented text structure and not bwh25's vector canvas**, verified against the help topic rather than inferred: bwh25 is a CAD surface (~60 Reed-Kellogg symbols, drag-to-connect, snap-to-grid, page setup), and its own overview says what shipped with it is "a fully diagrammed Greek New Testament (by Randy Leedy)" — **the half readers actually valued is licensed content we must not copy, so building the editor ships the half nobody praised**, while the editor is the half every review of Logos' equivalent calls clunky. It is also the wrong shape for *this* app specifically: the workbench is gated to tablets where there is no precise pointer, and half the corpus is RTL, which a symbol library must mirror wholesale — indentation is a flow direction, not a symbol, so it is RTL-safe for free. Three things worth carrying forward. **(1) The joints are proposed from the morphology already bundled**, which neither BibleWorks nor Biblearc does. Four levels, each a strict superset of the last, so changing level mid-study can only ever *add* a line — that monotonicity is what makes the level safe to touch. Verse starts break at *every* level because only 37.3% of tagged Greek verses begin at a clause joint, so omitting them leaves two thirds of verses running into the line above with nothing on screen distinguishing "the versification cuts this sentence" from "the tool is broken". **(2) Two invariants make the pane trustworthy, and both are counter-intuitive.** Breaks are stored as a *diff against the proposal* (`added`/`removed`), not as a line list, so a manual break survives a level change and a deleted break stays deleted when a finer level re-proposes it. Depths are stored *raw* and clamped to `previous + 1` only at layout, so outdenting a parent pulls its children up to follow without consuming the depths they chose — put the parent back and they spring back. Storing the clamped value would have flattened them permanently with no way for the reader to know a gesture aimed at one line had eaten another. **(3) A Semitic word is a stack and which morpheme you ask matters:** the conjunction and preposition are PREFIXES (first morpheme), the participle/infinitive is the HEAD — of 7,489 infinitives only 837 stand first, so a first-morpheme test misses 89% of them. **Trap that cost seven red tests: Greek morph codes are exactly ten characters.** `_isGreek` tests `c.length == 10`, so a hand-written `'C-------'` does not fail loudly — it falls through and parses as *Semitic garbage*, matching nothing. Every fixture in `phrasing_test.dart` is now attested in `assets/originals/*.json` with its corpus count in the comment. **Ship bookkeeping, twice-confirmed now:** the working tree was red (1 failure, `nasb-ev should be Simplified`) purely from the other writer's *uncommitted* Eagle's View rows; a detached worktree at the same commit gave **1325/1325 and a clean analyze**. Report the worktree numbers. The deploy was likewise made from that worktree, and the result was *verified* rather than assumed — `assets/nasb-ev.json` and `assets/nsn-plus.json` on dev return the 20 KB SPA fallback as `text/html`, while `bsb.json` returns 6.1 MB of `application/json`. Minor pre-existing quirk noticed but deliberately not fixed mid-run: `bump_version.sh` still greps for `const String kAppVersion = String.fromEnvironment(`, which `app_version.dart` no longer matches, so **only `kAppReleaseTime` gets stamped** and `kAppVersion`'s fallback sits at a stale `'1.3.113'`. Harmless today because every build passes `--dart-define=APP_VERSION` and `version.json` comes from pubspec, but it is a live trap for any build that forgets the define.
- **v1.6.33** — the version gutter sized to the longest label that exists, and one distinct opaque colour per version. *(Shipped and deployed by the preceding run; recorded here by the next one.)*
- **v1.6.32** — **the same original word, lit in every translation at once.** The word under study now draws every *other* printed occurrence of its lexical Strong's number in a green fill, across the Chinese line, the English line and the originals line simultaneously — so the reader can see that 万物 / "All things" / πάντα are one word, which is the whole reason a parallel view exists. **BibleWorks has no such feature, and the help proves the divergence is deliberate rather than an oversight on my part.** `bwh11_BrowseWindow` lists every highlight the Browse window can draw — Toggle Difference Highlighting (same-language versions only), Clear Hit Highlighting, manual phrase selection, the Diagrammer echo — and *none* of them is lexical. BibleWorks answers this question in a **pane**, not in the text: `bwh10_AnalysisWindow` says "The Use Tab shows a list of all occurrences of a word in the local context as the mouse is moved over words in the Browse Window." A list tells you *how many*; colour in the text tells you **which word over there is this word**, which is the only form of the answer that survives four parallel rows. Decisively, `bwh10l_AWUse` states "The Use Tab is currently not supported for double byte languages like Chinese" — the one reader this app exists for is the reader BibleWorks explicitly could not serve here. Four things worth carrying forward. **(1) The lit set is derived from the pane's subject, not re-decided.** `siblingStrongs()` takes the single value `_analysisWord` already resolves ("the pinned word if there is one, otherwise the last one hovered"), so the green words and the Analysis pane are *incapable* of disagreeing — there is one answer and both read it — and the echoes therefore follow the pin for free, staying lit while the reader walks the pointer off the text to read the pane. That is the entire point: a highlight that died on mouse-out would reintroduce the exact defect v1.6.28 fixed. **(2) The chapter gate needs its trailing separator.** A pin outlives navigation, so the lit set is gated on the `book|chapter` printed *now*; but `John|1` is a prefix of `John|11`, so testing the bare prefix leaks chapter 1's pin into chapters 10–19 — lighting words with no pinned word visible anywhere to explain the colour. Test it against `John|11`, not just against another book. **(3) Hue alone is not a third state.** `siblingBg` green and `selectionBg` blue measure **1.04 / 1.05 / 1.08:1** in luminance across the three palettes — for a red-green colour-blind reader those two fills are the same fill, which would have collapsed hover and echo into one. The fix is a second, non-colour channel: `wordMarkUnderline()` underlines only the reader's OWN subject (hover, pinned) and never an echo, so the three states are green-fill / blue-fill-plus-underline / blue-fill-plus-underline-plus-gold-border. It is a free function rather than three lines in the widget purely so it can be tested, since the widget cannot be. **(4) Threading the raw pointer position would have flickered, and only reasoning caught it** — no test and no screenshot would have. The Browse window threads the *latched* subject down as `hoverKey` and each word overrides it with `withHover()` only while the pointer is physically inside that word. Thread the live pointer instead and, in the 5px `Wrap` gap between two words, the latched subject matches its own `litStrongs`, falls through to `WordMark.sibling`, and every word the reader sweeps past blinks green on the way out. The rejected design is preserved as a named test so it cannot be "simplified" back in. **Data note, measured not assumed:** 2,432,524 word slots surveyed — the format is uniformly `[GH]\d+` with **no zero-padding anywhere**, so `==` is a safe comparison; 62,795 slots are empty, which is why "empty must never match empty" is its own test; and 86.6% of cuvs-plus numbers in a verse also appear in BSB's same verse, so the cross-row echo actually fires most of the time rather than being a demo-only feature. **Process:** verified by driving headless Chrome over CDP at 1280x800 and counting exact-RGB pixels rather than eyeballing screenshots — the Read tool downscales a 1280px capture to ~320px, which is far too coarse to see a 1.5px border, whereas a pixel census answers "did the echo light in N distinct row bands" objectively. One trap: a Flutter `Tooltip` overlay can *occlude* part of the browse column, so a before/after pixel delta will show phantom "new" highlights that are really just un-occlusion — diff the coordinates and look at them before believing the number.
- **v1.6.28** — **hover proposes, a click commits.** The Analysis pane followed the mouse, which meant it could not be used: hovering a tagged word filled the pane, and moving the pointer *toward* that pane crossed every word in between, so the content the reader was reaching for was gone before they arrived — unscrollable, unselectable, no click-through to a lexicon entry. The feature destroyed itself in the act of being used. BibleWorks has the identical defect and its own help admits it; `bwh10a` documents Shift-to-freeze "while you move the mouse cursor to the Word Analysis Window to use the scroll bar", and `bwh10l` adds a Freeze checkbox *and* a right-click "update only when shift key depressed" inversion. **Three escape hatches is evidence the default is wrong, not a feature set to copy.** Shift-freeze is also the wrong shape for this app: it needs a keyboard, on a build deliberately gated below 1072px so it can run on tablets *where there is no Shift key and no hover at all*; it must be held while the same hand moves the pointer; and it freezes the PANE rather than naming a WORD, so nothing on screen says what you froze — which is why BibleWorks draws no mark for it. So a click **pins**, and a pin is an *identity*, not a *mode*: it names one printed occurrence, it is drawn in the text, and while it stands the pointer may go anywhere without changing the subject. Shift-freeze stays, because it costs one boolean and a BibleWorks reader will try it out of habit. Four things worth carrying forward. **(1) The occurrence key must contain book and chapter, not just verse and index** — a pin outlives navigation, so without them pinning word 2 of verse 3 in John 1 lights up word 2 of verse 3 in John 2 the moment the reader turns the page: a gold marker on a word nobody chose, in a verse they have never seen. `versionCode` is in the key too, so the same Greek word on the originals row and on a tagged translation row are two distinct marks, which is correct. **(2) `pinMark` had to be its own palette colour.** The app's single gold `#C9A227` measures **1.54:1** against the paper theme's tan `selectionBg` — on 护眼纸质 the marker simply would not have been there. The three palettes now measure 3.98 / 6.67 / 4.13:1 and the test *computes* the ratio from sRGB luminance rather than hard-coding it, so a future palette edit that dims the marker fails the suite instead of shipping. Relatedly, every mark reserves the same 1.5px border and only the colour changes: a border that appeared on click would widen the word and reflow the line under the reader's own pointer, so the click would move the thing it had just committed to. **(3) Clicking a DIFFERENT word moves the pin rather than swallowing the tap.** This is the single most dangerous failure mode, because on a touch screen a tap is the only way to look at anything, and a pin that refused to move would strand a tablet reader on the first word they ever touched with the text apparently dead. Three exits, all verified: click the same word again, Esc, or the worded Unpin button. Nothing is pinned on first load; the empty state is untouched. **(4) Esc needs `HardwareKeyboard`, not `Shortcuts`/`Actions`,** which never fires — Flutter routes key events from the *focused* node upward and nothing is focused in a mouse-driven reading pane. The handler returns `false` so it observes without consuming, and is gated on `ModalRoute.isCurrent` so a standing pin cannot eat the Esc that should close a dialog above it. A latent bug fell out of this: `onWordTap` was passing the *workspace cursor* verse instead of the tapped word's verse, so crossRefs / stats / related / phrases / verseLists — everything routed through `_analysisVerse` — was filed under the wrong verse whenever the two differed; harmless while click was a minor gesture, load-bearing the moment click became *the* gesture. The callback now carries the whole `BrowseHover`. **Two process notes.** The state machine is a Flutter-free `AnalysisFocus` because it *had* to be: `browse_window_tagged_test` already documents that `FetchVerses.loadVerseList` never resolves under `flutter test`, so the Browse window sits on its spinner, no word ever renders, and **no widget test can reach this behaviour at all** — the wiring was verified by driving headless Chrome over the DevTools Protocol at 1280x800 (Node 24 has a built-in `WebSocket`, so zero installs). Two traps in that, both of which cost real time and produced convincing false "the fix doesn't work" results: `targets.find(t => t.type === 'page')` is *not* your app once Chrome opens an About tab, so filter targets by URL; and `Page.captureScreenshot` can return a **stale frame**, so wait ~2s after an interaction and re-shoot with no input before believing a bad result. Also: the pin opening the right pane reflows the browse column, so re-measure pixel coordinates from the current screenshot before every click instead of reusing them across a sequence.

- **v1.6.27** — **morphological search**, BibleWorks' Graphical Search Engine (bwh17), which the last research pass named the single biggest remaining gap. The reader could already see what a word *is*; it could not ask what else in the book looks like it. Now a ninth Analysis tab builds a query by clicking parts of speech and features, scoped to chapter / book / testament, seeded from the word under the pointer, over the MorphGNT (CC BY-SA 3.0) and OSHB/WLC (CC BY 4.0) tagging that was already bundled and licence-clear — no BibleWorks data anywhere near it. Three things are worth carrying forward. **(1) A Semitic word is a stack of morphemes, and it matches only if ONE morpheme satisfies EVERY constraint at once.** Asking each constraint of the word as a whole — the obvious implementation — returns 7016 "feminine verbs" in the WLC where there are 6129: 887 false positives, typically `HC/Vqv2mp/Sp3fs`, a *masculine* imperative wearing a feminine object suffix. That number is measured against the real text and pinned in a test, because the wrong answer looks entirely plausible on screen. **(2) Every offered value carries its live count, and a value that would return nothing is not offered at all** — the counts come out of the same single pass as the hits (a word lands in slot S's tally when some morpheme fails *at most* S), and the "what if I switched this one facet" promise was verified by actually re-running the switched query over Genesis and comparing totals, not asserted. **(3) The tab is APPENDED, never inserted:** `workbench.analysisTab` persists by INDEX, so reordering the enum silently moves every reader who had a tab open to a different one. Two data defects surfaced from surveying the whole corpus rather than reading the spec: 1068 Aramaic verbs were being given *Hebrew* binyan names, and OSHB's `x` "unknown or unnecessary" placeholder was being read as a value on 2684 words. The POS→slot map is likewise measured, not copied from the documentation — MorphGNT leaves personal-pronoun person blank, so `RP` genuinely has no person slot, and offering one would have produced a dead chip on every pronoun query. Performance needed no cleverness: 4037 distinct codes across 438,821 words parse in 18 ms, so memoising the parse makes every repeat query free (~100–300 ms warm across a whole testament). The pane keys on the *book*, not the verse — a query is the reader's work and must survive the pointer crossing a word — and splits into two scroll regions (facets capped at 45%, hits taking the rest) because a long facet list otherwise pushes every result below the fold, which is how the widget test found it. **Two process notes for whoever runs next.** The pure core (`morph_query.dart`, `morph_search_service.dart`, the `morphology.dart` refactor and three test files) is *in* `b21d17b`, labelled "chore(release): v1.6.26" and described by nothing — swept in by another writer's `git add -A` while it was still in progress. That is the second time; the rule in this repo is **stage exact paths by name**, and it is not optional. And this build was made from a scratch `git worktree` at the committed tree, not from the working copy: the working copy registers `assets/nasb-ev.json`, `assets/nsn-plus.json` and `assets/tagged/nsn-plus/` under its own comment saying never to deploy them, so `release_web.sh` run in place would have shipped restricted licensed texts to dev. Same reason the version bump here is staged as a single hunk of `pubspec.yaml` rather than the whole file.
- **v1.6.25** — the small-screen bar moved from two panes to three, on the owner's instruction (「手机不能打开三个栅栏，而我们的App是有三个Column，所以…不能打开三个的，全部都把它显示让他用我的另外一个软件」). Two columns was the wrong bar: search beside the text with no word analysis is not a smaller workbench, it is a reader with a search box, which is what YsWords already is and does better on a phone. The gate is now the three-pane minimum (1072 px) in either orientation, and the copy drops the two-pane figure entirely rather than naming a threshold the app no longer honours. Consequence recorded in the test rather than left to be discovered: **the classic 1024×768 iPad no longer qualifies** — 48 px short — so it goes to YsWords in both orientations, while iPad mini is portrait-blocked but fine in landscape (1133). Closing that 48 px means trimming the pane minimums (240 search / 480 reading / 320 analysis), which is a separate decision about how narrow a column may get, and was deliberately not bundled. *(Ship bookkeeping note: the code landed in `46bc7e5` and was deployed to dev as 1.6.25, but the version bump and this line were never committed — the run ended between `release_web.sh` and its "next: git commit + push". Recorded by the following run.)*
- **v1.6.24** — CJK font fallback on that language switch: 简 and 繁 were rendering as tofu. The labels used a bare `TextStyle`, which resolves to Roboto, and Roboto carries no CJK glyphs — so the two controls that exist *specifically* for Chinese readers were the only unreadable things on a screen they cannot otherwise get past. Fixed with `fontFamilyFallback: kCjkFontFallback`, the same chain the rest of the app uses for exactly this reason. Worth recording for every future run, because it generalises: **the tests could not have caught this.** They assert `find.text('简')` finds the widget, and it did — the string was present and correct, only its glyph was missing. Font coverage is invisible to the widget tree and shows up only in a rendered screenshot.
- **v1.6.23** — a language switch on the small-screen wall. Making the gate a hard block turned the interface language from a preference into a trap: Settings sits *behind* the wall, so a reader whose device is set to English but who reads Chinese would be stopped by a screen they cannot read, with no way to change it. The switch is the only exit, so it belongs on the wall. EN / 简 / 繁 as three flat buttons rather than a dropdown — someone who cannot read the wall cannot be asked to open a menu labelled in the language they do not read — and each label is written in its own script, so every reader can find their own. System language already worked (`AppSettings` seeds from `_detectSystemLocale()` on first launch and only a persisted choice overrides it); this adds the manual override the block made necessary. One paint-order bug the new tests caught: with the switcher first in the `Stack`, the centred content painted over it and ate every tap — visible, positioned correctly, and completely dead, with nothing about the layout looking wrong. It is last in the stack now.
- **v1.6.21–v1.6.22** — the small-screen gate stopped arguing with itself, then became a real gate. v1.6.17 gated on `min(width, height) >= 480` — "a phone is a phone in either orientation" — while the copy offered rotation as the one-tap fix. Those are incompatible: a phone's short edge does not change when it rotates, so no rotation could ever satisfy the gate the copy was promising to satisfy. A reader who followed the advice landed on "this screen does not reach two columns in EITHER direction … two need about 736. This screen is 844 x 390", which contradicts itself using its own printed numbers. User: 「把手机横过来并没有改变」. What decides whether the workbench works is whether the columns fit *right now*, so that is now the test — `paneCountFor(width) >= 2` means no advisory, and rotation is offered only when the other dimension genuinely crosses 736, which on a 360x640 phone it does not, and offering it there would be the same broken promise in a smaller size. Then, on the owner's instruction (「不要有仍然继续，就一直是block住」), `Continue anyway` was removed. I had argued the other way when this was specified, and that reasoning holds for a *reminder* — but this is not one: a one-column workbench is not a product the owner wants shipped, and every reader let past forms their opinion of SeekSparks on the worst version of it. The hard block is defensible rather than hostile precisely because there is a real destination behind it (YsWords, phone-first, linked directly) rather than a dead end. A dismissal persisted by v1.6.20 or earlier is deliberately no longer honoured — those readers are exactly the ones who met the self-contradicting advisory and tapped through it, so a stale bit would hold the gate open for the worst-served group. `onContinue` stays on the widget's API: it is what a future setting or a QA build would wire up, and removing it would make restoring the choice a larger change than the decision warrants.
- **v1.6.20** — the command line learned its **verbs** (BibleWorks bwh44, Command Line Shortcuts). v1.6.19 taught it to *search*; it could already navigate; but it still could not change what you were looking at or narrow where you were looking — which is most of what a BibleWorks reader does between searches, and all of it without touching the mouse. Now `d kjv` / `d -kjv` / `d c` / `d english` edit the Browse stack, `p kjv bsb` restacks in the order given, `l gen` / `l matt 5-7` / `l nt` scope the search and bare `l` lifts it, and a bare number is a verse in the chapter you already have open (`3:16` is chapter and verse in the book you are in). `l` is the one that closes a genuine functional hole rather than a fluency gap: `workbench_provider` carried a comment saying the workbench command line "always scans the whole Bible — book scoping lives in the standalone SearchPage", so until now the workbench literally could not search only the Gospels. Four things a naive reading of the help file misses, and they are where the work was: `d` is not one command but a **set editor with four arities** (add, remove, clear, add-a-whole-language); the **search version can never leave the display**, because BibleWorks defines `d c` as "clears all versions except search version", which makes that an invariant of the display and not a default anyone may override; `p` is not `d` three times — it carries an ORDER, which is the one thing the existing version-checkbox dialog cannot express; and a bare number is a **verse**, not a chapter, which is the opposite of what every other Bible app's jump box does. Routing the verbs *first* in `_submit` is safe for the same structural reason v1.6.19's grammar was: every verb is a single ASCII letter followed by whitespace or end-of-line, or a line made only of digits and a colon, and no bundled edition code, book name or query control character has either shape — pinned by a 19-input "not a verb" test group rather than argued, since the failure mode is silent theft of a search someone typed. Every verb echoes what it did, above the results rather than instead of them: a verb's whole effect lands in a *different pane*, which below the three-pane breakpoint is not even on screen, and `d bsb` must not throw away a hit list that took three commands to build. Refusals name the token (`d niv` → "No edition called \"niv\"" plus the list of ones we do have) because falling through to a text search for "d niv" would find nothing and teach a first-day BibleWorks migrant that the verb does not exist. Deliberately not implemented, each recorded with its reason: `d fav`/`o fav` (no favourites UI to name, and their absence removes the `d c` ambiguity), `f c`/`f u`/`f a` (no result checkboxes exist here), `t #` (the help's "current context tab" has no equivalent surface — mapping it onto the Analysis tabs would invent a meaning, and those numbers have already shifted eight times), and the command line's "never forgets" behaviour where ↑ also restores the search version of the recalled line. `l` deliberately stops at chapter granularity: verse granularity is the Verse List Manager's existing job, and its limits are already a corpus snapshot, so the versification caveat is unchanged rather than newly introduced. `parallelMode`/`parallelVersions` moved out of `workbench_page`'s State into `WorkbenchProvider` — a verb typed in the left pane has to reach state the centre pane owns, and a compiler-checked migration beats a published mirror that drifts. Logic in `utils/command_verb.dart`, Flutter-free; 70 core + 23 widget tests against a stand-in edition registry so adding a Bible cannot break the grammar; en/zh-Hans/zh-Hant throughout. One bug caught while writing the tests: the help strings shipped BibleWorks' own examples (`d nas`, `d 太`) and neither resolves here — `nasb` is the code and `太` is a *book*, not a version, so the on-screen help was teaching two commands that fail.
- **v1.6.19** — the command line finally speaks the BibleWorks query grammar (bwh16). The workbench pane has been *called* a command line since v1.0.2 while only ever doing a substring scan, so a reader arriving with BibleWorks muscle memory who typed `.love god` searched for that literal string and was told there were no results — and plain `god world` failed the same way for a different reason, because the scan strips spaces, so a two-word search could never match anything and never said why. Now: `.` AND, `/` OR, `'` lexical phrase, `;` linear phrase, `!` negation, `*`/`?` wildcards, `*N` word gaps, `;N` verse context. Three things a naive reading of the help file misses, and they are where the work was: `!` has two arities (a verse exclusion in AND/OR, but a one-word *position* inside a phrase, which is the documented `'!your *5 house` trap); `*` has two meanings (a character wildcard when glued to a word, a word gap when standing alone); and `;N` is a **verse** context, which quietly makes the window rather than the verse the unit of search. Three deliberate divergences from BibleWorks, each taken as the conservative option: a windowed hit reports *every* participating verse, not just the first, so no verse in the list lacks a search word; a NOT term excludes on the verse rather than the window, so widening a context never silently subtracts results; and the pane echoes the parse back in words ("All of: god, world"), which no competitor does — a grammar whose operators are punctuation is only safe if the reader can see what the punctuation was taken to mean, otherwise a wrong query is indistinguishable from a wrong Bible. The grammar is routed *first* in `runSearch`, which is safe precisely because control characters are first-position only, so no reference, version abbreviation or Strong's expression can be stolen from the branches below it — pinned by a test rather than argued. Highlighting had to learn the grammar too or every command result would have listed verses with nothing marked in them. Two real bugs found by testing against the actual 31,102-verse KJV rather than fixtures: `.?` matched the entire Bible (a bare metacharacter became an any-token slot; now gated on CJK presence, since English wildcards arrive as their own whitespace-delimited piece), and a pointed Hebrew query folded at parse time was compared against unfolded search keys, losing every hit before tokenization. Performance verified on real data, not assumed: `.melchizedek` tokenizes 2 of 31,102 verses; the worst realistic query `.lord god israel*;10` runs in 121ms, which is why the engine is synchronous rather than paying for an isolate hop. Also worth recording: the KJV spells the NT occurrences "Melchisedec", so that search returns 2 verses and not 9 — every corpus figure in the tests was measured, none invented. Logic in `utils/command_query.dart`, Flutter-free; 56 core + 13 real-corpus + 19 widget tests, en/zh-Hans/zh-Hant throughout.
- **v1.6.18** — Eagle's View import: three public-domain Strong's-tagged editions (kjvs KJV+Strong's/TVM, lxxwh Septuagint+Westcott-Hort in a new `grc` language group, cuvs-plus 和合本+Strong's). Decoder in tools/import_eaglesview.py. NASB/nsn+/RSV/NET deliberately excluded — redistribution not permitted. OPEN: ask the pastor about the AOSurvey conversion rights + the Modern Concordance index before importing it.
- **v1.6.17** — the small-screen advisory. SeekSparks is a fork of YsWords, the phone Bible reader; v1.6.9 routed phones into the Workbench for visual consistency and thereby made the real problem worse, because below 600px the workbench collapses to its centre pane and a phone got a menu bar over one column of verses — YsWords with extra steps, and a worse copy of its own parent. Silently degrading into the thing you are not is worse than saying "not here". So phones now get one screen explaining what the layout needs, and `Continue anyway` writes a flag that is never unwritten — an advisory, not a lockout, shown at most once ever. Gated on the SHORT edge, never the width: a phone in landscape is 844×390, wide enough to pass a width test but ~320px of text after the 68px of chrome, about eight lines. Rotation is only offered when it is actually true — a 360×640 phone rotated is 640 wide, which clears the app's 600px side-pane gate so two panes *do* appear, with the reading column at 384, under `minReadingPaneWidth` 480; the rule is therefore `portrait && paneCountFor(longEdge) >= 2`, and `paneCountFor` is the same helper the on-screen copy quotes, so rule and message cannot drift apart. Two ambiguities taken conservatively rather than guessed: Flutter web cannot reliably tell a phone from a short desktop window (iPadOS Safari reports as Mac), so the copy states the layout's requirement and the *measured* viewport instead of diagnosing the reader's device — actionable whether the fix is to rotate, resize a window, or move to a tablet; and the YsWords link uses the intl site for all three locales rather than routing zh-Hans to the China build, which would guess where a reader is from what they read. No error icon, no scolding — nothing has gone wrong. New in research this run, from bwh06's "Optimizing the BibleWorks Workspace": BibleWorks hit the same wall on "pad computers with limited space" and answered it the *opposite* way — hide the chrome (column control strips, toolbar, main menu, status bar) and keep all three columns, because chrome is affordances and columns are the product. That makes a hide-chrome toggle the highest-value follow-up (68px back ≈ 25% more text on a landscape phone); it was deliberately not bundled here. Logic in `utils/workbench_fit.dart`, Flutter-free, with the pane constants defined as sums of the real minimums so the figures shown on screen are the layout's own; 16 core + 15 widget tests, en/zh-Hans/zh-Hant throughout.
- **v1.6.16** — Vocabulary Flashcards (BibleWorks bwh40) as the eighth Analysis tab. Found ~2,700 lines of it already sitting uncommitted in the tree — the earlier 2026-08-07 run built it well and died before shipping, leaving the suite red because it had appended an eighth tab while `phrase_match_pane_test` still asserted `AnalysisTab.values.last == phrases`. That was the wrong invariant: lastness breaks on the next legitimate append, which is the one operation the test exists to bless, so it now pins the index instead. Finished and shipped that work rather than restarting it. The point of bwh40 is not flipping cards — it is that the deck is scoped to the passage you are about to read, so scope (chapter/book/corpus) is the primary control and the frequency floor is a published syllabus (Mounce 50×, Metzger 10×, Pratico & Van Pelt 70×/50×) rather than an invented ladder. The third mode `Read` is the Example Verse Finder, which twenty years of reviewers never once mentioned and which is the best idea in the module; its two knobs count differently on purpose — the minimum counts distinct lemmas, the maximum counts tokens. The drill does within-sitting repetition only; cross-day SM-2 needs dates and a scheduler and is deliberately a separate future feature, not a half-built one. Nothing licensed is copied: Strong's keys, public-domain glosses, frequencies measured from the bundled text rather than quoted. Added the 16 widget/service tests the dead run never wrote, against real 3 John Greek — note for future runs that `testWidgets` bodies run in a fake-async zone where real asset I/O never completes, so the caches must be warmed in `setUpAll`. Verified the code's own forum citation instead of trusting it: thread #3431 is real, but it has **12** archived posts rather than the "thirteen" the comment claimed and is ~5 users rather than an exodus — corrected before shipping. Research written up in `research-notes.md`, including the highest-value follow-up: STEPBible-Data (CC BY 4.0) would supply the lemma→part-of-speech mapping whose absence forced dropping that filter.
- **v1.6.15** — Phrase Matching (BibleWorks bwh51) as the seventh Analysis tab. Related Verses shipped two days earlier asks which verses share the most *words*; this asks which repeat the same *sequence*, because a bag of words cannot tell "thus says the LORD of hosts" from a verse that merely contains those five words scattered about — and finding who *quotes* a verse is a question only word order can answer. The gap is the feature rather than a tolerance: 1..G words may be inserted between phrase words, which is what lets Matthew's "and they shall call" still read as Isaiah's "and shall call", and G bounds each step rather than the whole phrase so a 3-word phrase cannot spread across half a verse. A dropped word is allowed too, but only an interior one — dropping an end word would silently turn a length-3 phrase into a length-2 one. Chopping a verse into 3-word windows leaves mostly filler and BibleWorks offers no answer to that, so phrases in more than 1% of what was scanned start unchecked with their verse count on the chip; Accordance's ">50% common words" rule was rejected because it discards "a virgin shall", which is the whole point of Isaiah 7:14. Verified against the real 31,102-verse KJV, not just fixtures: Jer 31:33 → Heb 8:10 then Heb 10:16, Ps 22:1 → Matt 27:46 and Mark 15:34, Isa 7:14 → Matt 1:23, each top of the list; worst case Esther 8:9 at 319ms. Logic in `utils/phrase_match.dart`, Flutter-free; 64 core + 6 real-corpus + 14 widget tests.
- **v1.6.14** — Verse List Manager (BibleWorks bwh27) as the sixth Analysis tab, plus the search-limit plumbing that is the point of it: every tool here already produces or consumes a set of verses, and there was nowhere to keep one. Two lists with an active-list radio, selection modelled as a composable *layer* (select-common + delete = difference, select-unique + delete = intersection) rather than a filter, and import that appends verbatim while `Sort list` is what dedupes. The filter toggle makes the active list the limit for subsequent searches (bwh29/bwh44 `l test.vls`), with a tappable banner in the command pane so results are never silently filtered. Versification remapping is deliberately absent — it needs licensed `.VMF` maps, so version is stored as provenance only and kept out of `VerseRef` identity. `Import ▸ From Document` scans prose for embedded references, which exposed and fixed a real pre-existing `reference_parser` defect where `Jude 14-15` yielded only Jude 1:14. Logic in `utils/verse_list.dart`, Flutter-free; 60 core tests + 11 widget tests.
- **v1.6.13** — Related Verses (BibleWorks bwh50) reaches the Analysis pane. The app already shipped curated cross-references, which is precisely why the *computed* version is new: it takes the words of the verse you are on, lets you uncheck the ones that carry no sense or weight one ×3, and counts how many each of the other 31,101 verses shares — so it finds the parallels no editor listed. Chinese gets overlapping character bigrams instead of a bundled segmenter. All the logic sits in `utils/related_verses.dart` with no Flutter import; 49 new tests, including a cross-check that the fast scanner agrees with the readable reference tokenizer.
- **v1.6.12** — removed the U+25A1 missing-glyph markers from 士师记 13:7 and 18:10 (and the mirrored traditional verses in 士師記). The two □ glyphs were a corruption of the reverence space CUV prints before 神 — every other occurrence in the file is normalised away. Added a regression test that verifies both CUVS-YHWH editions have exactly 31,102 verses with no U+25A1, and spot-checks the two fixed verses carry the canonical text.
- **v1.6.11** — 护眼纸质 reaches the workbench. The paper theme used to stop at BibleReadingPane's content subtree; chrome, the parallel Browse window, and every `WbColors.of` call site stayed on the neutral desktop palette. Added `WbColors.paper` + a `paper:` flag on `workbenchTheme`, watched `AppSettings.readingPaperTheme` in `WorkbenchPage.build`. Also added value equality to `WbColors`.
- **v1.6.10** — search hits are now marked in the Browse text itself, not only in the verse list. A hit list without marked hits is a table of contents.
- **v1.6.9** — the chrome (menu/toolbar/status bars) is present at every width. Phones read as the same tool with side panes collapsed, not as a different app.
- **v1.6.8** — finished the `WbType` migration: 82 hardcoded size call sites reduced to 5. Settings sliders (Font Size, Menu Size, Line Spacing) now drive the workbench.
- **v1.6.7** — Settings actually reach the workbench: 2 of 10 user settings honoured before, now all of them through `WbType.of(context)`.
- **v1.6.6** — stripped the NASB pilcrow (¶) that was being printed as scripture, without touching the actual verse text.
- **v1.6.5** — stopped printing publisher markup (StripePosition tags etc.) as if it were scripture.
- **v1.6.4** — Word List Manager + search-hit distribution. Two BibleWorks analysis tabs backed by real data.
- **v1.6.3** — Key Word In Context (KWIC), straight from the BibleWorks feature set.
- **v1.6.2 (BSB)** — added the Berean Standard Bible, the only English translation here with Strong's tagging. Closes the gap that left the English parallel row untagged.
- **v1.6.1** — release plumbing fix.
- **v1.5.2** — fix: Compare-two-versions actually compared two versions instead of duplicating one.
- **v1.5.1** — release plumbing fix.
- **v1.5.0** — per-word Strong's on the Chinese line (和合本雅伟版 + BDB/Thayer).
- **v1.6.52** — **the product audit, and three pages nobody can open (task #281).** The brief framed this as *redundancy*; it is mostly **reachability** and **modality**. **The audit's test comes from the help file, not from taste**, because the obvious frame — *YsWords/devotional = cruft, BibleWorks = real* — is provably wrong: BibleWorks 10 ships a Timeline (bwh39), Maps (bwh33), flashcards (bwh40) **and Bagster's Daily Light (bwh52), a daily devotional**. What its modules share is that each **round-trips to the text** — the Timeline *"jump[s] directly from an event … to a Bible reference"*, Daily Light *"exports to the Search Window the list of verses"*, and bwh03 says the whole program is *"heavily cross-linked"*. So: **does this screen take you to the text, or bring you back holding something?** That test earns its keep by disagreeing with the naive one twice, both corrected by reading code rather than filenames: **`stats_page` is not engagement metrics** (`grep -c "streak\|daysRead"` = **0**) but top-lemma tables, per-book originals distribution and hapax legomena off `concordance.json` — bwh23/bwh26 territory wearing the name most likely to get it cut; and **`bible_trivia_page` is not a quiz** (no score, no timer) but 68 curated entries on acrostics, the Tetragrammaton and numeric structure, each jumping to its reference. **The headline finding: `DashboardPage` is referenced by nothing outside itself.** `main.dart` made `SmallScreenGate(WorkbenchPage)` the root deliberately — *"SeekSparks is a Bible STUDY tool, not a devotional"* — but nothing was pointed away from the Dashboard afterwards, so it and the two pages reachable only through it, `FamilyTreePage` (2,620) and `FeedbackPage` (468), went dark together: **4,508 lines**, plus a live Settings section that reorders a page nobody can reach — ~6,000 lines hanging off one question, *does SeekSparks have a home screen?* **The identity decision was already made and then not finished.** Found by BFS over the **import graph** from `main.dart` (255 files in `lib/`, 221 reached); now enforced by **`test/page_reachability_test.dart`**, whose allow-list *is* the review list, executable. **1,628 tests passed with all of it unreachable** — same lesson as v1.6.48: every test that touches those pages constructs them directly, so no unit test can notice that nothing else does; an invariant about the graph *between* units must be asserted about the source. **Traps if you touch that test:** conditional imports (`import 'x_stub.dart' if (dart.library.io) 'x_io.dart';`) span lines with the real target inside the `if` arm — miss them and every platform-split service reads as an orphan; and stop scanning at the first `class `, or doc comments quoting `.dart` become phantom edges. **Deleted ~1,714 lines, all grep-proven, nothing user-visible:** stats_page's three dead tabs (they have carried *"safe to remove in a future cleanup pass"* since Round 56), `parallel_verse_view.dart` (superseded by `browse_window`), `bible_stats_service.dart`, 28 `ui_strings` keys for reading plans removed in v1.2.69, `AppSettings.pickVerseAfterChapter` end to end — *a preference whose only effect was to be remembered* — `greeting.dart`, and `path_provider`+`crypto`, which the pubspec itself said to drop if a second consumer never came (fifteen months, none). **Near-miss worth carrying: nearly deleted a fix.** `version_colors.dart` had **zero** references and was created by 3cf2f0a to fix the version-pill colour drift; first read was *"a shipped fix was never wired"*. Wrong — it was written for `parallel_verse_view` and died with it, and its guarantee **survives** in `kVersionTagColors`/`wbVersionColor()`, which are fully opaque and keyed on the version **code**. **When a file is unreferenced, ask whether it is redundant or whether it was never connected — grep cannot tell those apart and they mean opposite things** (bears directly on #285's warning that `kVersionTagColors` is keyed on labels). Likewise `liquid_glass.dart` looks orphaned but is imported by the orphaned `feedback_page`, and deleting it broke `analyze`: **transitive dead code is not independently deletable.** **Sharper diagnosis than "redundant": modality** — surfaces that should be simultaneous are full-screen pushes, the same defect #276 fixed for the Reader and #284 will fix for 原文逐字. **`search_page` (2,877) is a second search UI** with AI mode, recent searches and a scope banner the Workbench pane lacks, so **#280 is blocked behind merging them** — a scope model built twice will drift, and it already has. Then `stats_page` → Analysis tabs, then the `map_viewer_page` vs Places (#277) duplication. **Deliverables:** `docs/PRODUCT-AUDIT.md` and `docs/DELETION-REVIEW.md`, the latter holding back everything the policy reserves for a human — the Dashboard cluster, the two search implementations, the map duplication, and **#278 bundled NASB, flagged and untouched**. **Verification:** analyze exits **0**, **1630/1630 green** (+2), deployed **v1.6.52** and probed on the immutable deploy URL — `pickVerseAfterChapter`, `planResetProgressConfirm`, `ParallelVerseView`, `BibleStatsService`, `planJumpToToday` all **absent** from `main.dart.js` while live symbols remain, and a 1440px screenshot shows the workbench booting three-pane on Jonah 1 (NASB), which is the real check on the edited `AppSettings` load path. **Not verified:** stats_page's surviving tabs were confirmed by compiler + analyze only, not screenshotted — reaching them needs Reader mode and several clicks.
- **v1.6.53–v1.6.54** — **the chrome pass reaches Settings, and gets a ratchet so it cannot un-happen (task #279).** v1.6.53's own entry was never written — that run was killed by the watchdog after committing and before deploying — so this covers both. **The finding that changes the shape of the whole 13-page pass belongs to v1.6.53 (commit 49242dd): the pills were never in the pages, they were in the theme.** `workbenchTheme()` builds from a fresh `ThemeData.light/dark` and returns `base.copyWith(...)`, discarding `main.dart`'s per-widget themes, and it only ever named **three** components — card, input, tooltip. Everything else rendered on stock **Material 3 defaults, which are stadium-shaped and elevated**. *No page ever asked for a pill; it asked for a `FilterChip`.* Squaring the components in the theme (all five button families, chips, sheets, dialogs, menus, snackbars, AppBar, tabs, list and expansion tiles, the progress bar) fixed a large share of #279's 67 sites everywhere at once, and cannot drift. Switch, Slider and the checkbox keep their radius — those are affordances, not chrome. **So re-count before estimating the rest: #279's per-page counts were taken *before* the theme was fixed.** Measured on `settings_page` after it: of the **54** sites the ticket lists, **19 were `Card(` and are now square for free**; what was left were **16 inline sites the theme cannot reach** — plain `BoxDecoration`s, `ToggleButtons.borderRadius`, an `OutlinedButton.styleFrom(shape:)`, two `ClipRRect`s and a `CircleAvatar`. The remaining twelve pages are likely far cheaper than the ticket implies. **Three calls the theme's own carve-out did not decide, each taken on a reason rather than a feel:** the two progress bars were `ClipRRect(radius: 2)` wrapped around a `LinearProgressIndicator` — not affordances, and the theme *already* sets `progressIndicatorTheme.borderRadius: BorderRadius.zero`, so they were **overriding the rule they should have been following**; the **Google sign-in button** is square now, because Google's branding guidelines fix the wordmark, the logo and the white/`#DADCE0` pair and **corner radius is not among them**, so it keeps everything actually specified (the instinct to treat a third-party button as untouchable *in whole* is what needed correcting); and the **colour swatches were `CircleAvatar`s** — a swatch is a *sample*, not a control, its round shape carries no meaning, and **bwh29's own "Program Colors" / "Morphology Colors" sections use square ones**. Selection there had been signalled only by an overlaid check, so it now also gets a 2px border, since hairline-vs-2px is how the rest of the workbench says *this one*. **The new mechanism is a ratchet, not a checklist: `test/page_chrome_pass_test.dart`.** The failure mode of a thirteen-page pass is not a missed page, it is a **converted page quietly growing a rounded card back a week later** — and no widget test can catch that, because `BorderRadius.circular(10)` renders perfectly and passes every assertion about *behaviour*; it is wrong only against a rule that lives in prose. Same species as v1.6.52's `page_reachability_test` — **an invariant about the source that no unit test can see.** Its `_passed` list is the pass's progress bar: a page joins it in the iteration that converts it and can never silently leave, with a sibling test blocking the cheapest way to make it pass (emptying the list). Comments are stripped before scanning so prose about the rule cannot fail the rule. **Negative-tested by injecting an offender**, which is the only way to know a source-scanning test is doing anything. Density untouched throughout, per the brief: it is the *chrome* that must match, not the line height. **Deliberately not attempted, and it is the bigger half of the Settings problem:** bwh29 shows BibleWorks' Options Window is **one window with a section list on the left**, not a long scroll — *"To move to a different section in the options, click on the item in the list on the left."* SeekSparks' settings is a single 4,286-line scroll with anchor keys that scroll-to-position. That left-list shape is the structural answer to "4,286 lines for 12 tiles", and it is **not a chrome change** — it belongs to #281's size question. **Correction to the brief, for whoever takes #280:** it says to first verify whether `l 创 限定检索范围` is a phantom because grepping for `searchScope`/`scopeBooks` found nothing. **That grep is stale.** v1.6.20 shipped the `l` verb, and `workbench_provider.dart:70` carries `Set<String>? searchLimit` with a `searchLimitLabel` beside it — **verified in code, not taken from the handoff.** So #280 is **surfacing and extending, not building from zero**. And bwh29 adds a requirement the brief omits: limits can be *"define[d] … by name for later recall"* — **named, saved scopes are part of what the feature is**, so build the model with a name field rather than retrofitting one. **Verification:** analyze exits **0** (by exit code), **1665/1665 green**, deployed **v1.6.54** and **screenshot-verified live at 1440×900** — the Profiles "Local only" badge renders **square with a hairline** (magnified 8× to check, because at 1:1 a 12px radius and a 0px radius are one pixel apart), the seven Primary Colour swatches are **square**, and the Paper/Carbon style-preset rows are **square**. Before/after pairs taken on the same surface. **Trap that cost real minutes and will cost them again:** the **first load of the main dev URL after a deploy serves the previous bundle from Flutter's service worker** — `version.json` already said 1.6.54 while `main.dart.js` was still 1.6.53, so the after-screenshot showed the *unchanged* pill and looked like the fix had not shipped. `Network.setCacheDisabled` does not touch a service worker. **Navigate twice.** Also: the immutable deploy URL is a **different origin**, so it has no saved settings and boots into the small-screen gate rather than the workbench — fine for string-probing `main.dart.js`, useless for screenshotting a page you have to navigate to. And a stale `Emulation.setDeviceMetricsOverride` from an **earlier session** survived on the target and pinned `innerWidth` to 320 while `Browser.setWindowBounds` reported 1440; `Emulation.clearDeviceMetricsOverride` on a *new* socket did not clear it — re-asserting the override with an explicit `setDeviceMetricsOverride` did, and it then held across clicks, contrary to the v1.6.51 note. **Check `innerWidth` with an `eval` step before trusting any screenshot's width.**
- **v1.6.55–v1.6.56** — **the chrome pass reaches the Bible Evidence archive, and bwh10 argues *against* flattening it (tasks #279 + #283).** Target chosen by re-deriving priority rather than trusting the ticket's counts: family_tree (21) and dashboard (15) are heavier but **frozen behind #281's unanswered "does SeekSparks have a home screen?"**, and search_page/map_viewer are merge-and-duplicate candidates, which leaves `evidence_page` (11) + `evidence_detail_page` (8) as the heaviest **actionable** pair — and the audit's verdict on them is *Keep*. **The design question was whether the archive should stop being a gallery**, since `stats_page` had just been converted on the principle that *a frequency table is a table*. **bwh10 settles it the other way, and this is the first time in the pass that the help file blocked the flattening instinct.** The Mss tab is a verse-linked image surface whose *entire* help topic is image **processing** — unsharp mask, sharpening, despeckle, grayscale, reverse, histogram stretch — every verb existing so the user can see the artefact **better**. BibleWorks does not shrink a manuscript image into a row; it treats it as a **specimen you examine and hands you a darkroom for it.** So the grid and the photographs stay at size and only the framing goes — which kills the hero's 12px `ClipRRect` on a stronger ground than "the theme says square": **rounding an artefact photograph crops the artefact.** The test for #279 is therefore not *is this decorative* but ***what is this surface FOR***. **Second confirmation of v1.6.53's lesson, now a rule:** the two search fields' `OutlineInputBorder(borderRadius: 10)` were **overrides fighting a theme border that is already square** — same shape as the settings progress bars — so when a page looks unconverted, **grep for overrides of what `workbenchTheme()` already sets before writing any new styling; the theme is usually right and the page is arguing with it.** **#283 was bundled in deliberately, because restyling a header that prints the wrong text is not worth shipping:** six surfaces printed the archive's raw English keys to a reader on a Chinese version. **Two findings generalise beyond this page.** (1) **A localisation bug can be structurally impossible to fix at the call site** — `uiStrings['evidenceForBook']` was `'经文实证 — '` with **no `{book}` placeholder**, so the page concatenated a raw name because the template gave it nowhere to put a localised one; when auditing for #283, **grep the strings table for entries that look like prefixes, not just the call sites.** (2) **A parameter can be accepted and never used for months**: `_ReferenceChip` — the single control on the detail page that round-trips you back to the text — has taken a `locale` argument since the day it was written and never passed it anywhere, and **`dart analyze` cannot see this**, because the field *is* used, by the constructor. `test/evidence_localization_test.dart` asserts on **rendered text, not on the mapper**, since `localeAwareBookName`/`localizedReferenceLabel` were always correct and only the *call* was missing; **mutation-checked** by reverting the `_ReferenceChip` line, which fails 2 of its 5 tests. It also pins the rule a naive fix would break — `bookScriptFor` says the **reading version** picks the spelling, **not the UI locale**, so a Chinese UI on KJV must still print `Genesis`; a fix keyed off `locale` passes the Simplified and Traditional cases and fails only that one, so that case is its own test. **The 320px screenshot the brief mandates earned its keep and produced v1.6.56:** the detail page's metadata chips sit in a `Wrap`, and **a `Wrap` does not shrink a child that is already wider than the line**, so `Shrine of the Book, Israel Museum, Jerusalem` overflowed at 320 **and at 390, which is a real phone**. The bug predates the pass; **what changed is visibility — a soft tinted pill just faded off the edge, where a hairline box that crosses the edge reads as broken.** Generalise it: **squaring a surface and giving it a 1px border will surface pre-existing overflow that a borderless chip hid** — budget for it on the remaining pages, and take the 320px shot before calling one done. `EvidenceDetailPage` now sits in `responsive_all_pages_smoke_test.dart`, which had `EvidencePage` but **not the page you reach from it**, and its fixture carries the long location **on purpose**, because with a short one the test passes against a page that still overflows in production — **a responsive test is only as good as the width of its longest string.** **Found and deliberately NOT fixed:** `assets/bible_evidence.json` carries **188 double-encoded sequences across 96 of its 225 entries** (`â\x80\x93` where an en dash belongs — `'1946â€“1956'`), UTF-8 read as Latin-1 and re-encoded, almost certainly from the Vite/React migration the archive was ported out of; it renders as visible garbage in the timeline and discovery-date chips. It is a data change to a shipped asset touching 96 entries — its own slice with its own verification — and folding it into a chrome pass would have made both harder to review. **Verification:** analyze exits **0 by exit code**, **1674/1674 green** (+9), deployed **v1.6.56** and **screenshot-verified live at 1440 and at 320 on the shipped bundle** — cards and hero images flush in hairline frames with no radius or shadow, `WbPanel` header strips on the detail sections, the reference chip square, and at 320 the location chip now ellipsises inside the column. **The #283 fix was verified in the running app, not only by test:** with the reading version switched to `cuvs-yhwh` **while the UI stayed in English**, the card and the chip both print **以赛亚书 40:8**, which is precisely the reading-version-wins rule. **Two new screenshot traps.** **Chrome clamps a real window to a 500 CSS px minimum** — a `Browser.setWindowBounds` of 320 reports `innerWidth` 500 — so emulation is unavoidable for the narrow shot; but **`Emulation.setDeviceMetricsOverride` at a width much larger than the real window makes Flutter lay out against the emulated dpr**, rendering the whole UI at a different CSS scale (the 21px menu bar measured ~9) while `innerWidth` reads the requested value the entire time, so **the usual `innerWidth` check does not catch it and every click coordinate taken from an earlier screenshot misses.** Working recipe for a two-width run: **resize the real window and drive all navigation with the override cleared, then apply the override only for the narrow capture, where no clicking is needed** — and `Emulation.clearDeviceMetricsOverride` *does* work when sent on the same socket that set it (v1.6.54's "did not clear" was a cross-session case). Also: **SeekSparks gates below 1024px**, so an emulated-320 navigation lands on the small-screen advisory, not the workbench — the narrow shot of any inner page can only be reached by navigating wide first and narrowing after. **Dead end, do not repeat:** there is no BibleWorks help topic for an evidence/archaeology archive — SeekSparks' has **no analogue at all**, which `docs/PRODUCT-AUDIT.md` already correctly reads as an argument *for* it.
- **v1.6.57** — **there is no home screen, and a Resource has to bring you back (tasks #281 + #279).** The item was the user's Decision A on `docs/DELETION-REVIEW.md` §1: delete the Dashboard cluster, **keep the Family Tree** and move it under **RESOURCES**. The deletion is the easy half; **the half that was not in the brief is that every "open in Reader" in the app had silently stopped returning to the workbench**, and it was the *same* principle that found it. **bwh07 splits Tools from Resources on one question — does the item OPERATE on the current text, or is it a reference database you CONSULT?** Tools = Word List Manager, KWIC, Phrase Matching, Related Verses, Synopsis, Diagrammer; Resources = Commentaries, Grammars, **Maps**, **Pictures/Bible Views**, X-Refs, Dictionaries, Lexicons. `assets/family_tree.json` — 277 people with dates, relations and citations, **none of it derived from the open chapter** — is unambiguously the second kind, so it goes in Resources. The same test settles **Decision C** in advance: one map surface under Resources, while verse-linked "which places" stays in the Analysis pane, **because that one does operate on the current text**. **SeekSparks' menus violate the taxonomy today and I deliberately did not re-sort them** — Evidence, Timeline and Trivia sit under Tools but are databases; "Notes & highlights" and "Go to book…" sit under Resources but are user data and navigation. No defect behind it, so it is a recommendation, not a change. **The defect: `MaterialApp.home:` gives its widget `Navigator.defaultRouteName` (`'/'`).** #276 made the Workbench the root (`home: _RootRouter(...)` → `SmallScreenGate(WorkbenchPage())`), but `navigateToReader()`'s predicate only knew `'/HomePage'` and `'/Workbench'`, so the `popUntil` walk **fell through to the root, stopped on `route.isFirst` without setting `foundExistingReader`, and pushed the CLASSIC single-pane `HomePage` ON TOP of the Workbench** — command line, search results and Analysis pane gone, the reader **ejected out of the workspace #276 had just folded it into**. `isFirst` is now **part of the answer, not a fallback**: the bottom of this stack IS a reader — a claim about `main.dart` strong enough to need pinning, because the day someone adds an onboarding wall the predicate starts **lying** and every "open in reader" becomes a **silent no-op instead of an eject**, so a test reads `lib/main.dart` and asserts the root still resolves to `WorkbenchPage()`. **The defect class is the point: behavioural correctness hiding a structural loss.** A `HomePage` renders the requested verse **perfectly** — right chapter, right highlight, right scroll — so every assertion anyone would write (*"does tapping this citation show me Genesis 12"*) passes. **What was lost is not the verse, it is the workspace**, and no unit test has an opinion about which of two correct-looking screens you are on. Third instance of the same species after `page_reachability_test` and `page_chrome_pass_test`: **an invariant that lives between units, asserted about the source because the units are individually fine.** **Second-order lesson, a real dead end: the reachability test over-approximated and HID this** — all seven offenders import `home_page.dart`, so the reader read as reachable from everywhere. **Reachability says "an edge exists"; it cannot say "the edge is the wrong edge",** and a graph invariant that only answers *can you get there* will not notice that the way you get there **destroys the thing you came from**. **Seven pages had each independently grown the same body** — prepare the jump, then `pushPage(const HomePage())` — highlights, evidence, evidence_detail, trivia, timeline, person_detail_sheet, strongs_entry; the helper existed the whole time. **That duplication is why one wrong predicate broke the round-trip in seven places at once**, so `test/reader_round_trip_test.dart` bans the pattern at source, carving out only `navigate_to_reader.dart` (owns the one legitimate push) and `open_reader.dart` (deep-link entry, **runs before any navigator stack exists, so it has nothing to pop back to**). **Trap, hit for the second time: the ratchet fired on `home_page.dart`'s own doc comment**, which described the old pattern in prose — both wrong, so the comment was rewritten to say what is now true *and* the scanner strips comment lines; **a ratchet that fires on its own explanation gets deleted rather than fixed.** Assume every new source ratchet needs it. Incidental proof the centralisation was total: repointing the seven sites left **12 unused imports** (`home_page.dart`, `app_nav.dart`, `get.dart`) — **nothing still needed to know the reader's class name**, a stronger statement than the test makes. **Deleted ~3,700 lines:** `dashboard_page` (1420), `feedback_page` (468), `feedback_service` (171), `liquid_glass` (764), `dashboard_section`, both `browser_info_*` platform stubs, settings' 235-line `_DashboardSectionsCard`, 18 `ui_strings` keys, and the dashboard order/visibility fields on `AppSettings`; **`_knownOrphans` in `page_reachability_test` is now EMPTY** — the allow-list *was* the review list and the review is done. **Kept on purpose: the SharedPreferences purge in `resetSettings()`, written out as literal strings** (precedent: `'offlineMode'`), because **deleting a preference's code does not delete the preference from users' devices** and a reset that leaves `dashboard_section_order` + seven `dashboard_section_visible_*` keys behind is a reset that does not reset. **Verification:** analyze exits **0 by exit code**, **1671/1671 green** (+5, −3 deleted-page smoke entries), deployed **v1.6.57** and **screenshot-verified live at 1460 CSS px through the whole round-trip** — Resources dropdown → Family Tree → 277 people, groups expand → Abraham's sheet lists its verse references → tapping **Genesis 12** lands back on the **three-pane workbench** with the centre pane on 创世纪 12, menu bar and search and Analysis panes intact. **That last frame is the whole iteration: before the fix it would have been a single-pane HomePage.** **Not attempted, now unblocked by the same decision:** #279's chrome pass on `family_tree_page` (21 sites, was frozen behind the home-screen question), and Decisions **B** (one search), **C** (maps into Resources) and **E** (Chinese version labels — remember `kVersionTagColors` is keyed on LABELS, not codes).
- **v1.6.58** — **the chrome pass reaches the family tree, sheet included (tasks #279 + #283).** *(Entry written one run late — v1.6.58 shipped and deployed but its run ended before documenting itself. The code is in `13bfd09`.)* The heaviest page left on #279's list and the one #281's Decision A unblocked by keeping the family tree and moving it under **Resources**. **The rule the page produced:** era colour survives on **ink** — a coloured title, a 2px left rule, a tag — and dies as **decoration**: gradient header fills, alpha washes, tinted badges. That is the same test `evidence_page` passed for photographs; the workbench tolerates a saturated thing when it carries information. `WbPanel` gained `onTap`/`accent` and `WbTag` gained `onTap`/`icon`, which retired five bespoke pill widgets (`_RolePill`, `_SpouseChip`, `_RefBadge`, `_BridgeChip`, `_NextEraTag`). **Two traps worth inheriting.** `era_palette`'s new `eraColorFor` keys on `WbColors.isDark`, **not** `Theme.brightness`, because under the paper palette those two disagree and a brightness-keyed lift washes every era out on a cream ground. And the `Theme` has to wrap the **State**, not `build`: `_showDetail` opens the person sheet from `State.context`, so a `Theme` applied inside `build` sits *below* it and the sheet comes up in the app palette behind a converted page — which is exactly why `page_chrome_pass_test` now lists the sheet alongside the page. **Two bugs found on the way:** `_BridgeLeafRow.isRecentlyJumped` was declared and never read, so the jumped highlight never appeared on bridge leaves; and `person_detail_sheet._localizedRef` omitted the reading version (#283), so a reader on a Traditional edition tapped a Simplified book name and landed on a Traditional page — one book, two spellings, inside a single round-trip. It delegates to `localizedReferenceLabel` now, which also stops dropping everything after the first semicolon in a multi-book reference.

- **v1.6.61** — **the phone splash hang, and the widget the brief blamed was innocent.** Prod v1.6.56 sat on the splash forever on an iPhone. The named suspect was `small_screen_advisory.dart:71`, the `_dismissed == null` blank `Scaffold`; but that screen is empty and surface-coloured and the reader was looking at the *splash* — logo, 寻光, a verse — which is **upstream of the gate entirely**. `SmallScreenGate` wrapped `WorkbenchPage` **inside** the post-splash router, so no fix within it could reach a phone that never got past `LoadingPage`. **The root cause is one sentence: whether a viewport can carry the workbench depends on nothing the boot produces, yet the answer was sequenced behind all of it.** The gate now wraps `home:` and is answered on frame one. **Three defects fell out of that.** (a) A phone parsed every bundled Bible before being told to use YsWords — 6-15 s of splash under a 12x CPU throttle, unbounded on cellular. (b) The gate blocked on a `SharedPreferences` read whose value **could not change the answer** — `Continue anyway` was removed in v1.6.20 and the gate has passed `dismissed: false` unconditionally ever since, so the whole round-trip was dead async work whose only observable effect was a blank frame; deleted, along with `kSmallScreenDismissedKey`. (c) **The genuine forever-hang, and it is not phone-specific:** `LoadingPage._scheduleAdvanceIfReady` arms the 3 s hand-off and may only arm it once verses exist, but v1.2.28's build-time safety net set the one-shot flag **before** calling it, so a build arriving while `verses` was still empty spent the shot on a no-op and nothing re-armed. That is the *normal* ordering whenever main.dart's 4 s watchdog beats the active version's fetch — a cold cache or a slow connection — and the v1.2.28 comment block describes this exact bug as the thing it fixes, reintroduced one line above itself. The flag now belongs to the callee and means "the Timer is armed". **`_autoHardReload` could not save it:** its `stillStuck` test asks `bootInFlight || _retrying || loadError != null || verses.isEmpty`, and in the stuck state the boot had finished perfectly — a liveness check written against the failure you imagined will not catch the failure you got. **Why 1715 green tests missed it:** on a warm desktop the verses are in hand before `LoadingPage` mounts, so the racing build never happens, and every gate test pumped the widget directly rather than through the router, making its *position in the tree* — the actual defect — untestable by construction. `test/splash_advance_test.dart` drives the real ordering and was **confirmed to fail against the old code** before being accepted. **Reproduced and verified by screenshot, not by suite:** CDP `Emulation.setCPUThrottlingRate: 12` on an emulated 375x812 (network throttling does *not* work — a cold re-download delays the 4 s timer's practical start too, closing the race) showed v1.6.56 on the splash at t≈6 s and v1.6.61 on the advisory at the same instant; the advisory paints by t≈1 s unthrottled. 1024x768 re-checked for the big-screen path — three panes, unchanged. **Shipped to dev AND prod** (pre-authorised for this bug); prod jumps 1.6.56 -> 1.6.61 and so also carries v1.6.57-1.6.60.
- **v1.6.62** — **one worldwide build, and the largest Google dependency was not in the Dart.** SeekSparks shipped two bundles differing by a compile-time flag whose entire job was to skip the parts of boot that talk to Google, because those hang behind the GFW. **The flag was the workaround; this removes what it worked around.** Deleted: `firebase_core` / `_auth` / `cloud_firestore` / `firebase_database` / `google_sign_in`, `CloudAuthService` (657 lines), `RealtimeDbSyncService` (908), the never-initialised Firestore `CloudSyncService`, the web plugin registrants, `CloudSetupDiagnostic`, `SetupInstructionsCard`, `GoogleGLogo`, `build_flags.dart` and every `kChinaMode` branch, plus 78 orphaned ui-strings and the sign-in UI in Settings / reading pane / About / onboarding. **The change that matters is one removed `await`:** `main()` used to await `Firebase.initializeApp()` + `getRedirectResult()` behind an 8 s cap, so a reader in China paid that timeout before a single verse was parsed. **Safe to delete because every local write was `prefs.setString(...)` FOLLOWED BY `requestUpload()`** — persistence never depended on the sync layer, so no data moves; the only sync→local path was the BYOK Gemini key, which only ever filled an empty value. `google_fonts` went too: it downloaded 14 of the picker's options from fonts.gstatic.com, i.e. the picker offered a dozen choices that were dead for exactly the readers this release is for. **An unknown font key does not throw — it resolves to nothing and renders the default**, so `migrateLegacyFontKey` retires removed keys in two passes (the legacy CSS table pointed `Georgia`→`Lora`, and Lora is now gone too), and six style presets that hard-coded `Inter`/`EB Garamond`/`Merriweather` were repointed; `test/font_catalog_migration_test.dart` locks both and the preset check was **confirmed to fail against the old values**. **The finding that reframed the item:** Flutter's bootstrap fetches CanvasKit (~1.5 MB, nothing paints without it) from `www.gstatic.com` — `flutter build web` already emits the same files locally and simply does not use them. `--no-web-resources-cdn` in `release_web.sh` + `build_web.py` fixes it; **without that flag the rest of this work would have been cosmetic.** **Verified by measurement, not assumption:** CDP network capture of a cold boot on dev = 21 requests, **all same-origin, zero Google hosts**; Chinese (创世记 12) and Greek (Ἐν ἀρχῇ ἦν ὁ Λόγος, polytonic diacritics intact) both screenshotted rendering correctly. `flutter analyze` clean **by exit code**; 1703 + 6 new tests green. **One dependency remains and it is measured, not guessed:** opening a Hebrew word study pulls four fonts from `fonts.gstatic.com` (incl. `notosanshebrew`) via Flutter's engine-level `fontFallbackBaseUrl` — pre-existing, unrelated to this change, and it means **Hebrew would render BLANK in China**. Bundling an OFL Hebrew face as an asset, exactly as `NotoSansSC-Sub` already does for CJK, is the next item. Also unresolved by choice: the catalogue has **no serif left**, so `reverent` / `reader` / `paper` are carried by spacing and material alone. Dev only; prod not authorised.
- **v1.6.64 / v1.6.65** — **a version code outlives the version, and nothing was checking (item -3, the prod crash).** `https://seeksparks.netlify.app/#/genesis/1?v=cuv-yhwd` died on `FormatException: Unexpected token '<', "<!DOCTYPE "...`, and the message named none of the chain: `cuv-yhwd` was removed from the catalog with its asset in **180ba8b**, boot restored the code from SharedPreferences without validating it, `FetchVerses` asked for `assets/cuv-yhwd.json`, and **Netlify answered an unknown path with the SPA fallback — `index.html`, status 200** — which `rootBundle.loadString` returned quite happily for `json.decode` to choke on. **On the web a missing asset does not 404 into an exception**, so the symptom reads as a *corrupt* Bible rather than an *absent* one; it also means any "does this asset exist?" check by HTTP status is worthless on this host (`curl` the retired asset and you get 200 and ~20 KB of app shell — the same 20,266 bytes that proves `nasb-ev.json` did *not* ship). **Live since the removal, not a v1.6.62 regression**, and prod had been rolled back to v1.6.61 on that wrong hypothesis, surrendering the 6.1 MB bundle win for nothing — **check the date of the removal against the date of the rollback before believing a regression story.** **Wider than one code:** `cuv`, `cuv-tr`, `cnv`, `cnv-tr`, `cuv-yhwd`, `biblexg`, `biblexg-tr` (pinned from `git log --diff-filter=D`, cross-checked against `bible_versions_language_test`). A version code is written to prefs on every chapter change, into every share link as `?v=`, and into the persisted Browse stack, so **retiring an edition strands bookmarks, synced devices and anyone who simply had it open**; five entry points could carry a stale code, **only two were guarded, and one of those keyed on `bibleVersions` rather than `availableVersions`** — a version hidden via `disabledVersions` would have *passed* the check and then failed to load. **The brief named `?v=` as the suspect and it was already guarded**; the genuinely unguarded path was `MainProvider.restoreState`, which assigned `savedVersion` straight to `currentVersion` behind two hardcoded special cases — so the real-world mechanism is that the reader opened `?v=cuv-yhwd` while it still shipped, the code was persisted, and **every boot since crashed before the first verse painted**, a bug that worsens with time and that a fresh browser cannot reproduce. **The brief did not mention the fifth entry point:** the persisted Browse stack (`workbench.parallelVersions`), restored in two places and consumed in four more. `retiredVersionSuccessors` maps each retired code to the surviving edition **preserving language AND script** — `cuv-yhwd` → `cuvs-yhwh` is the identical text sharing all 31,102 references, so that reader loses nothing, and handing a 繁體 reader 简体 is **not a smaller version of the right answer, it is an unreadable one** (pinned by test). An unlisted code falls through to the fallback, so a future omission **degrades quietly instead of crashing**. `resolveReadingVersion` takes the fallback as a **parameter, because only the caller knows it**: boot wants the locale default, a deep link wants the edition already on screen — **a stale link is a reason to ignore the link and never a reason to move someone off the Bible they were reading.** **Guarded at the point each list lives, not at each restore site:** `WorkbenchProvider.parallelVersions` became a getter/setter that runs `loadableVersions` on assignment, so the page, the command line and the version picker are covered by one rule — **filtering per call site is exactly how this came to be validated on some paths and not others** (it also sidestepped an edit conflict with another writer live in the tree; the better design and the available one coincided). `FetchVerses` now refuses an HTML payload with an exception **naming the asset** and **skips the retry ladder** for it — a missing file will not appear on the third attempt, and the escalating 20/40/60 s timeouts spent a minute of the reader's boot to reach the same answer. A substitution is not an error but not nothing either, so `RetiredVersionNotice` tells the reader once, **above the router**, catching both the boot path and the deliberately-unawaited `UrlSyncService` path. **Verified live, not assumed:** analyze **0 by exit code**, **1772/1772 green** (+29), and both entry points reproduced in headless Chrome against the real deploys by seeding `localStorage['flutter.version']` — **prod v1.6.65 boots Genesis 1, rewrites the stored code to `cuvs-yhwh`, and shows the notice, with zero JS errors** captured via a `preload` hook on `error`/`unhandledrejection`; the Parallel view renders with retired codes seeded and the garbage entry dropped. **Item -2 discharged in the same push** (pre-authorised, gate run from a clean tree at the pushed SHA): prod `version.json` = 1.6.65, **`main.dart.js` 4,836,473 bytes — down from 10,965,398, the 6.1 MB the rollback had undone** — `nasb-ev.json` and `nsn-plus.json` both return the 20,266-byte SPA fallback, and Settings → Export produces a real payload stamped 1.6.65. **Trap worth inheriting:** CDP clip coordinates are **CSS pixels** but screenshots come back downscaled; reading menu coordinates off a 600px-wide image of a 1440px viewport clicks at 2.4× the wrong place. **Cost named:** `retiredVersionSuccessors` is hand-maintained, so retiring an edition still means remembering a row — two tests reduce that to a failing suite rather than a crash (every successor must be loadable; no retired code may also be live), and a forgotten row still degrades to the fallback.
- **v1.6.66** — **one scope model for search *and* analysis results, and the fix was a denominator rather than a label (task #280).** The scope already existed and already narrowed the left search pane; what nothing did was carry it into the **result lists** — KWIC and the Analysis stats pane ran over the whole corpus no matter what the banner said, so `G2962 · 64 处` was ambiguous about which of the two numbers it was, and a reader with a limit set could read a count that silently contradicted his own filter. **bwh23 answers the ambiguity better than a label does: BibleWorks prints the scoped statistic *beside* the whole-version figure**, so the count is now `inScope / total` (`11 / 46`) with an `N in all — limited to X` sub-line, which is strictly more informative than naming the scope and removes the need to trust the banner. Not copied verbatim: BW writes `*64 (1278)`, and **the asterisk is a footnote marker with no footnote** — it means "limited" only to someone already taught, and it does not travel to a Chinese UI. **The line for *which* lists obey the scope is bwh10l's, not an oversight:** lists that enumerate *hits in the corpus* (KWIC, word stats) honour the limit; lists that *describe* a corpus (Context word lists, Vocabulary) and reference databases (cross-refs, places) deliberately keep their own range — BibleWorks' Use tab ignores verse limits by design and carries a range of its own, because "how often does this word occur in Mark" and "what words does Mark use" are different questions and only the first has a scope. **A latent #283-class bug fell out of the wiring:** `LimitSpec.label` is canonical **English** and no display site re-localised it, so `l 创` set a scope that printed *"Genesis"* into a Chinese banner. The provider now keeps the **spec** as the scope's identity and one `scopeDisplayName(spec, locale, version)` renders it everywhere — book names follow the **reading version**, not the UI locale, so every display site is fixed at once instead of three of them being fixed by hand. **Terminology per the brief — 希伯来圣经 / 希腊圣经, never 旧约 / 新约** (same reasoning as the 雅伟 restoration); `oldTestament` / `newTestament` survive only as **label keys**, and `l ot`, `l nt`, `l 旧约`, `l 舊約`, `l 希伯来圣经` all still parse, because **you do not take away the word someone types in order to change the word he reads.** A test asserts no string on the scope path contains 旧约/新约/舊約/新約. **The core is pure and the sheet has a pinned contract:** `lib/utils/search_scope.dart` (groups, canonical order, books↔spec, `scopedCountLabel`) with 21 tests, plus 6 that pin the picker's three-state group chip and — the distinction that would otherwise rot — that **dismissing returns `null` (cancel) while "Clear all" + Apply returns a non-null EMPTY set (clear the scope)**; `wholeBooksOfSpec` returns null when a range names chapters, so `l matt 5-7` is shown but not silently rounded up to Matthew. **Two traps worth inheriting.** `setEquals` lives in `package:flutter/foundation.dart` and **`material.dart` does not export it**. And `cond ? uiStrings['k']?[locale] ?? 'fallback' : (…)` **does not parse** — the null-aware index inside a conditional produced five errors (`non_bool_condition`, `dead_null_aware_expression`, `argument_type_not_assignable`, `missing_identifier`, `expected_token`), **not one of which names the real cause**; an `if/else` assigning a `final String` is the fix, and the analyzer's error text is worth distrusting whenever `?[` and `?:` meet. **Verified on the deployed build by screenshot, not by suite:** the banner `Limited to Pentateuch (5852)`, the status-bar field `Limits: Pentateuch`, the stats pane reading `1 / 3`, `2 / 20`, `495 / 2127` under *"In scope / in all, rarest first — limited to Pentateuch"*, and the KWIC header `H1254 · 11 / 46 hits` over `46 in all — limited to Pentateuch`; a narrow pass at `innerWidth 1044` with the sheet open showed no overflow. analyze **0 by exit code**, **1799 green** (+27). **Three things deliberately not done, so nobody re-derives them:** the scope is **session-scoped and does not survive an app restart** — the brief asked for persistence *across searches in a session*, and a limit that outlives the window is exactly the "you may have forgotten you have search limits set" failure bwh09 warns about; the 旧约/新约 inside long historical prose in `ui_strings.dart` (~1017–1131, 2275–2417, e.g. "旧约的七十士译本") was **not** find-replaced, because rewriting quoted history is an editorial pass and not cleanup; and bwh29's **named saved limit selections** plus bwh16's **"Make Search Results New Search Limits"** — the loop that turns a result set into the next scope — remain unbuilt and are the natural next items. Dev only; prod not authorised.
- **v1.6.67** — **the order of the columns is part of the comparison, and BibleWorks answers "which versions" and "in what order" in two different windows (task #288).** The ticket read as *the parallel-version picker is a checkbox list*; the defect is larger than its styling. **A column of checkboxes cannot express an order**, so the picker re-sorted the reader's stack into registry order every time it was opened — `p bsb kjvs lxxwh` on the command line set an order that the only pointer-driven editor then silently destroyed, and nothing asserted which of the two was right because the order had no test. **The help file is why the sheet has two sections rather than one.** bwh09's *Choose Display Version(s)* is a checkbox tree and answers selection only; **bwh29's *Version Display Order* is a whole separate window** of Move Up / Move Down / Move to Top / Move to Bottom whose result saves to a named `.vdo` file — and the tell is its **Show Active Only** checkbox, which is evidence that ordering is only tractable over the subset you have already chosen. bwh38's Parallel Versions Window folds both into Available → Add → up/down → OK. Hence a draggable **"In display order"** list stacked over an **"Available"** catalogue, not one list wearing both jobs. **Staged with Apply/Cancel, not live-apply:** a drag is far easier to do by accident than a checkbox, applying parses an edition's JSON, and both BibleWorks equivalents end in OK — whereas the old sheet applied on **dismiss** and had no other way to apply, so there was no way to back out of an edit at all. That is neither model, and it is the actual bug. `null` means CANCEL, following `search_scope_sheet.dart` from v1.6.66. **The reading version is drawn but inert** — no grip, no remove — because the invariant is inherited rather than invented: `applyDisplayVerb` seeds every stack with the search version, the command line refuses `d -kjv` (`cannotRemoveSearchVersion`), and `command_pane` strips it before storing; the count is of **columns**, so it includes it. The model is a Flutter-free `lib/utils/version_stack.dart` (normalise / display / toggle / move) so the contract is testable with no widget tree, and every entry point goes through `normaliseComparisons` — which is also what keeps a stale `cuv-yhwd` in SharedPreferences from a pre-retirement build out of the stack, i.e. the **v1.6.64 crash class**, where a missing asset comes back as Netlify's `<!DOCTYPE` SPA fallback. **`ReorderableListView.onReorder` is deprecated in favour of `onReorderItem`**, which adjusts `newIndex` for the lifted row itself, so `moveComparison` takes its indices at face value and the classic off-by-one — invisible on an upward drag, wrong on every downward one — now lives in exactly one place, the framework's. The widget test drives `onReorderItem` directly rather than simulating the gesture (the gesture is Flutter's to get right; the index convention was ours) and reads the resulting order off the rows' **screen positions**, so it asserts what is painted rather than what is in the draft. Fixed in passing: the old sheet enumerated `bibleVersions` rather than `availableVersions`, so it would have offered `disabledVersions` — that set is empty today, so nothing user-visible changed. **Verification:** analyze exits **0**, **1827/1827 green** (+28), and **screenshot-verified live on seeksparks-dev at v1.6.67** at 1440×860 and again at **1000×700, the narrowest layout there is** (below ~992 px the small-screen gate takes the screen, so there is no 320 px case to photograph): locked reading row with lock icon and the word "reading", three draggable rows with grips and remove buttons, the Available catalogue grouped by language, and a pinned hairline action row reading "4 versions displayed" / Remove all / Apply — square corners, hairlines, no shadows throughout. End-to-end at the minimum size: adding LEB appended it to the **end** of the display order and to the pane's last column rather than dropping it into registry order, reopening the sheet showed the persisted order intact, and removing it restored the stack. **Only assumed, not verified live:** an actual pointer **drag** — the reorder is pinned by test through `onReorderItem` but was never performed with a mouse in the browser — and the Chinese surface strings. **Deliberately not built:** named `.vdo` order files, `<o name>` recall, and bwh38's Parallel Versions *Favorites*; note for whoever picks that up that the same "named, recallable configuration" gap now exists in **two** places, #280's saved search limits and #288's version favourites, which argues for one manager rather than two. **Trap for the next run, and it cost an hour:** `Page.navigate` to a URL differing from the current one only in the **hash fragment** is a *same-document* navigation, so the document never reloads and the screenshot shows the **old bundle after a successful deploy**. The service worker was innocent (`getRegistrations()` and `caches.keys()` both returned 0) and so was the server (`curl` of the deployed `main.dart.js` found 11 occurrences of the new version) — navigate to a cache-busting **query** instead, `/?cb=1667#/john/1:1`. And contra v1.6.51's note, `Emulation.setDeviceMetricsOverride` did survive `Input.dispatchMouseEvent` on this build, provided it is re-issued in the same step list.
- **v1.6.68 / v1.6.69** — **"What is the NEAR5? I clicked on it and nothing happens" was three defects, and only the third one is the sentence the reader wrote (task #294).** The brief's first instruction was to establish whether the buttons are BROKEN or merely OPAQUE, because the previous run's harness never reached the Flutter field and its reproduction proved nothing. **They are both, and the broken part was invisible to 1849 green tests.** (1) **Two chips emitted queries our own parser rejects:** `G25` + `✶` produced `G25 * ` → `parseStrongsBoolean` returns **null** → silent fallthrough to a text search for the literal string, and `!` produced `G25 !G26` → also null. The `✶` case is the sharpest — **`G25✶` is the form the shipped help documents and it was the one query the `✶` button could not make.** One glue rule now gets all three documented meanings right (`G25`+✶→`G25*` prefix wildcard, `.faith`+✶→`.faith*` character wildcard, `'信心 `+✶→`'信心 * ` word gap), and the parser splits a glued `!` back off so the same keystrokes mean NOT in both grammars. (2) **v1.6.63's "one search" merge regressed discoverability:** the deleted `search_page.dart` hid the combining bar until a Strong's number was present and gave every chip a localised tooltip; `command_pane.dart` showed them unconditionally with **none**, the `?` card documented **only the text grammar** (press `?` to ask what NEAR5 is, get ten lines about `.love god`), and a shipping string still promised the old behaviour ("they appear once you type a number") — that key and 7 siblings were deleted after grep proved them unreachable. The strip now **dims rather than hides** the combining operators (a button that vanishes as you type cannot be learned from) and answers a tap on a dim one with a hint row instead of silence. (3) **The actual "nothing happens" — the browser's select-all echo, found only by driving the deployed build over CDP.** Tapping a chip blurs Flutter web's hidden `<input>`; putting focus back makes the browser **select ALL of it**, and that select-all is echoed into the framework as the new editing state: after typing `G25` the DOM reads `sel (3,3)`, after tapping `NEAR5` it reads **`"G25 NEAR5 "` `sel (0,10)`** — so the next character REPLACES the query the button just helped build. `G25` → tap `NEAR5` → type `G26` → **the line reads `G26`**, and Enter runs a plain lexicon lookup. It applied to **every chip**, not just NEAR. Every write to the line now goes through one `_writeLine` that arms a guard undoing exactly that signature — same text just written, wholly selected, once — so a select-all the reader made himself is untouched; **rejected a post-frame re-assert** because the echo arrives from a DOM event on its own schedule and can land after the frame. **`flutter test` never has a DOM input, which is why the suite was green for the whole life of the bug**; the pin that works replays the echo through the platform channel (`testTextInput.updateEditingValue` with `TextSelection(0, 10)`) and was **confirmed to fail with the listener removed**. Generalise it: **any "clicking X does nothing" report about a control that writes to a text field should be checked for caret state over CDP before the insertion code is touched** — the insertion code here was never broken, and a run that trusted the report's wording would have rewritten the one part that worked. **The parity decision, taken conservatively and stated rather than implied:** bwh16 writes proximity as **`*n` BETWEEN two words inside a phrase** (`'faith *3 christ`) — which SeekSparks already ships (`cmdSyntaxGap`), so the question was only ever about the *Strong's* grammar — and **`*n` was NOT adopted there**, for three verified reasons: `*` is already the prefix wildcard so `G25 *5 G26` sits one space from `G25* 5 G26`; **their `n` counts INTERVENING words while our `NEARn` counts word-position DISTANCE, so NEAR5 ≡ BibleWorks `*4`** and an alias would silently shift every window carried over; and **theirs is DIRECTIONAL, ours is not**. The `?` card now states that difference. **Ordered proximity remains a real, named parity gap** — deliberately not closed, because inventing a second operator name would add confusion to the one task whose purpose is to remove it. Also shipped: the distance is **editable** (the parser always took any `NEAR<n>`; only the button was pinned to 5), with `−`/`+` rewriting the token in place, preserving the reader's own spelling of `NEAR`/`WITHIN`, clamped to the parser's own 1–50 — and the handler takes a **delta read off the live line**, because one that closes over the build-time distance steps once for two quick taps (that was a genuine bug the test caught). Pure core in `lib/utils/command_draft.dart` (`analyseCommandDraft` / `withNearDistance` / `describeCommandDraft`), whose one subtlety is that **only a line made of NOTHING BUT operators is read as a button press** — uppercasing every token and looking for `AND` turns `.love and god`, an ordinary English search, into a nag about Strong's numbers. **Verified live on seeksparks-dev at v1.6.69, not in the suite:** the strip dimmed on an empty line and bright after `G25`; tapping `NEAR5` giving `G25 NEAR5 ` with `sel (10,10)` and the hint "needs a second Strong's number after it, e.g. G26"; typing `G26` now **keeping** the first number; `+` twice giving `G25 NEAR7 G26` with button label and hint agreeing; and **Enter returning 7 verses** (约翰福音 15:9, 17:26) — the whole chain end to end. `flutter analyze` **0 by exit code**, **1851 green** (+24). Dev only; prod not authorised.
- **v1.6.70 / v1.6.71** — **a total says how often; it does not say where — the word-distribution chart (task #290).** The ticket's load-bearing instruction was wrong, and the whole feature turns on catching that. It said the chart must be **Greek-only** and **absent for Hebrew**, because the Stats tab's existing source, the Eagle's View Greek profile, returns null for every Hebrew number; it also called `analysisStatsHint`'s *"Whole-Bible occurrences"* a defect for 27-book data and asked for it fixed in three locales. One inspection of `assets/strongs/concordance.json` disposes of both: **14,039 entries, 8,640 H + 5,399 G**, each entry's per-book map `b` **summing exactly to `n`** — uncapped, complete, both testaments — while the reference list `r` beside it stops at 500 and counts distinct VERSES. `analysis_tabs.dart` was **already calling that service once per word and throwing `byBook` away**, so the Hebrew chart cost one field, not one service; and the hint string is *correct*, since the figures it describes are the concordance's, which are whole-Bible — **left unchanged on purpose**. The one thing the ticket got right it got right for the right reason, and the data proves it: `G3056` is **330 occurrences across 317 verses**, so the footer says *"Counted by occurrence, not by verse — one verse may carry the word more than once"* rather than borrowing bwh23's *"# vss in bk with hit"*. **The same numbers are drawn twice, and the show-zeros question is answered oppositely in each — because it is a question about width, not taste.** bwh23 defaults "Show Zeros" off and offers a **New Window** command conceding that a 66-category chart *"allows you to display more of the graph at once"*; so the labelled chart in the centre pane drops empty books and states their absence as *"In 24 of 66 books"*, while the **label-less strip in the 256–560 px Analysis pane must keep them** — with no book names, a bar's POSITION is the only thing that names its book. `G5357` (φιάλη, 12, Revelation only) settles it on screen: one bar at the far right of an empty ruler reads as "Revelation" with no text at all. The strip is a `CustomPainter`, not 66 boxes in a `Row`, because fixed-width children at the narrow end of a resizable pane is precisely where RenderFlex overflow hides. Rows run down, bars grow rightwards — bwh23's default, since its "Swap Axis" is what turns names horizontal — with canonical / most-first order from its "Sort Books", the reader's current book outlined, and rank-in-book printed only where it exists (Greek) rather than zeroed for Hebrew. **The defect the screenshots caught and 1,862 green tests did not:** at 1440 the header read `λόγος G3056 330`; at **992** it read `φιάλη… 12` — word and number shared one `Flexible`, so the ellipsis ate **the number**, which is the header's entire job given the chart outlives the tap that opened it. v1.6.71 moves the number outside the `Flexible`: the word truncates, the number cannot. **Deliberately not built**, each because the honest version needs data the repo does not have: bwh23's normalised `%: hits / words in the book` (no whole-Bible per-book word count exists — an invented denominator is a plausible wrong chart), row→verse navigation (`refs` caps at 500, so it would half-work silently on exactly the common words), and a scoped chart (it is whole-Bible and **says so** under an active limit, which is bwh23's parenthesised-asterisk admission in fewer pixels). **Found in-area and left for its own ticket:** `analysis_tabs.dart` ~L492 computes `inScope` from `result.refs` — distinct verses, capped at 500 — and hands it to `scopedCountLabel` beside a `total` that is whole-Bible occurrences, so one ratio prints **two different units** and silently truncates for any word in more than 500 verses. Incidental fix carried along: `search_stats_strip.dart` had a hardcoded `"OT … · NT …"`, English in a Chinese UI (#283's class) naming the corpora in the terms #280 settled against — now `希伯来圣经 / 希腊圣经` via `uiStrings`.
- **v1.6.72** — **the sermons had no author on any page that showed them (task #291).** The ticket said the credit was "partly present" and missing from the detail view; it was worse than that. `sermonsTagline` ("Pastor Eric Chang's sermon library") was in `ui_strings.dart` and **rendered by nothing** — the only grep hit was its own definition; the library header said just 讲道; the detail page showed id, date, passage and topic chips and never said whose preaching it was; and `_copySermonBody` put forty paragraphs on the clipboard under a "From SeekSparks" footer with **no byline**, so quoted sermon text left the app unattributed. The name rendered in exactly one place — a Settings offline-pack row that spelled it a fourth way. Three unrelated `ui_strings` entries, three spellings: the drift the user reported was the symptom, not the disease. **BW's answer is bwh10i, the Version Info tab: provenance follows the focused resource, and the copyright lives inside the version's own `.bww` rather than being retyped in the UI.** So provenance belongs beside the text and gets **one** definition. `assets/sermons/index.json` has no author field and adding one means rewriting 289 records for a constant, so **`lib/constants/sermon_credit.dart` plays the part of the `.bww`** — the name is written three times total, once per locale (following `sermon_topics.dart`'s precedent) — and the library header, the detail byline, the clipboard payload, the About row and the Settings row all compose from it via `preacherName()` / `sermonLibraryCredit()` / `withPreacher()`, with `{name}` templating keeping the two `ui_strings` entries that need it from spelling it again. **The ratchet:** a test fails the build if any `uiStrings` value contains 张熙和 / 張熙和 / "Eric", and a companion test fails a template that carries `{name}` in one locale but not all three — the quiet version, where English gets the byline and the Chinese locales silently lose it. Restraint is deliberate: **one line per surface, never a list row** — crediting all 289 rows would be advertising, not attribution. **Corpus arithmetic, measured not trusted:** the row said "587 sermons"; the index holds **289 records** whose `parts` sum to **589**, in up to 3 languages for **867 files**. 587 was reaching for the part count and stating it as sermons, and the code comment at `offline_pack_service.dart:28` was not even self-consistent with itself (587 × 3 ≠ 867). **NOT changed, and it needs a human:** About credits the sermons "© 梁家铿", who is the **translator of the biblexg edition** (`aboutLicenseLjk` = 圣经释经事工), not the preacher — unexplained since the initial commit `6f13365`. He may be the publisher who granted permission, which would make the line correct standing beside the new byline; **deleting a rights claim on a guess is worse than carrying an unverified one**, so it stands, flagged in a code comment. **Trap for the next chrome-adjacent change:** `main.dart:608/699` paints the AppBar `lightScheme.primary` / `darkScheme.primaryContainer`, so an AppBar subtitle written as `scheme.onSurface` is nearly invisible — use `appBarTheme.foregroundColor`. Caught by reading the theme, not by the analyzer and not by 1,871 green tests. Verified by screenshot in all three locales (CanvasKit renders a missing glyph as **absent text**, so 張/師 had to be seen, not assumed).
- **v1.6.73** — **"bundle an OFL Hebrew face" was 869 uncovered code points, and Hebrew was 79 of them (task #289).** The measurement had to come before the fix: unioning the cmaps of the only two fonts the app bundled (Roboto **922** code points, NotoSansSC-Sub 6,017) and subtracting them from every character in `assets/**` + `lib/**` gave **CJK 384, Greek Extended 209, Hebrew 79, emoji 70, Hangul 23, transliteration diacritics ~50**. **Roboto's Greek is MONOTONIC**, so all 209 assigned code points of U+1F00–1FFF — every breathing and accent in the Westcott-Hort NT and the LXX, i.e. the app's own subject matter — were being drawn by a font downloaded from **fonts.gstatic.com**, and the inherited v1.6.62 research note asserting "Roboto has Greek but not Hebrew, so Hebrew is the uncovered case" is **wrong**. Hebrew was only the *reported* symptom because a word study is where a reader meets a bare lemma. The download is not app Dart and not `google_fonts` (gone in v1.6.62): it is the **Flutter engine**, which fetches a Noto face from `fontFallbackBaseUrl` for any code point covered by no *registered* font — which is also why v1.6.62's boot audit came back clean while a word-study click did not, the fetch being lazy and glyph-triggered. **The 384 missing CJK characters are the sharper finding: `tools/build_cjk_font_subset.sh` hardcoded `/Users/pliu0036/Documents/yswords`, the fork PARENT**, so the subset had been built against the wrong repository for months and the gap was precisely the text that exists only here — `thayer_zh.json`, `bdb_zh.json`, the zh-TW sermons, i.e. the Chinese lexicon glosses that are this app's differentiator. Nothing caught it because **CanvasKit renders an unresolvable font as ABSENT TEXT, not tofu**: the failure is silent and total, and a reader behind the Firewall saw a gloss with words missing rather than a row of boxes. A prior run had already met this and documented the wrong conclusion — `workbench_theme.dart` carried "fontFamily deliberately NOT pinned … none of those carry Hebrew. Leaving it unset lets the engine reach its own broader fallback", which is a description of *depending on the download*; the comment is fixed alongside the cause. `tools/build_font_subsets.py` replaces the shell script and builds all four subsets in one pass, **unioning each with the cmap already on disk so a rerun can only add coverage** — the property whose absence let the fork-parent bug survive a regeneration. **Hebrew and Greek are subset BY RANGE, not by scan, because they are also INPUT** — a reader can type ἀγάπη or אהב into the command line and a scan-derived subset blanks on the first form no asset happens to contain; CJK stays scan-derived only because the full face is 16 MB. **The bundling line is "everything a reader reads":** scripture, lexicon, transliteration, and the search syntax the command line prints in its own help — but **not decorative emoji**, since a monochrome symbol face would displace the colour glyphs iOS and macOS already draw, a native regression bought with a web fix, and the discriminator is visible in the data (the decorative glyphs in `bible_evidence.json` carry U+FE0F, the load-bearing ones do not). Five symbols cleared that bar into their own 1 KB file, including the **□ that stands in for a character lost from Judges 13:7** in `cuvs-plus.json` — when it vanishes the verse silently reads as though nothing were missing. **Chosen so native cannot change:** the three families sit AFTER the OS font names in `kCjkFontFallback` and in both of `main.dart`'s theme chains, so the system faces still win every glyph they have and only CanvasKit — where those CSS names are unresolvable — ever reaches the bundles; `NotoSansExt-Sub` is additionally built as `EXT − Roboto's cmap`, a pure gap-filler, so chain order cannot alter a glyph that renders today. **Conservative call, stated rather than implied:** this reproduces today's *mixed* rendering offline (Roboto for unaccented letters, Noto Sans for accented) rather than improving it — making original-language typography *consistent* means pinning a family on original-language spans, which changes how Greek looks for every existing reader, and that is a typographic decision, not a side effect of a networking fix. Fixed in passing because it was load-bearing: the stats page's **Aramaic row carried a SYRIAC badge** (`ܐܒܓ`) contradicting its own comment — biblical Aramaic is written in the Hebrew square script — and it was the last glyph in the UI still able to trigger an engine font download. `test/bundled_font_coverage_test.dart` parses the sfnt cmap subtables (formats 4/6/12) in pure Dart and asserts real coverage plus declaration-and-chain membership, with **two guards against passing vacuously** (Roboto alone must parse to exactly 922 and must NOT contain א or ἀ; the asset scan must see >5,000 distinct characters) — necessary because **a widget test never loads a font**, which is why 1,879 green tests could not see any of this. **Verified live on seeksparks-dev at v1.6.73 by screenshot, twice over:** with `fonts.gstatic.com` blocked, Genesis 1:1 renders fully pointed (בְּרֵאשִׁית בָּרָא אֱלֹהִים … with niqqud and cantillation) and John 1:1 renders full polytonic (Ἐν ἀρχῇ ἦν ὁ Λόγος … breathings, circumflex, iota subscript, grave) alongside Traditional and Simplified Chinese, and the Hebrew word study — the ticket's exact scenario — opens with transliteration `(rê'shîyth)` intact. **Then again with the requests HANGING rather than blocked**, via `Fetch.enable` with no `continueRequest`, because the Firewall drops packets and a blocked request fails *fast* while a dropped one stalls until timeout — identical render, so nothing is on a blocking path. **Remaining and deliberately not closed:** a word-study click still fires **3 speculative `notosanssc` shard requests**, and downloading those exact shards and diffing their cmaps proves **every code point in all three is already bundled** — the engine asks for fonts it does not need. `fontFallbackBaseUrl` would end them, but pointing it at a dead same-origin path would also **kill web emoji for everyone** (88 distinct emoji live in `bible_evidence.json` / `maps_index.json` and reach the screen only via that download), so the trade needs its own ticket rather than a guess. **Also found while measuring, not fixed:** `assets/bible_evidence.json` ships **154 mojibake sequences** (132 `â€“`, 3 `â€”`, 19 `Ã©`) — visibly corrupt text in prod from a UTF-8-read-as-Latin-1 round trip — and the command line's help prints `G25✶` while the parser accepts `G25*`, so a reader copying the help types a character it rejects. `flutter analyze` **0 by exit code**, **1879 green**. Dev only; prod not authorised.

- **v1.6.74** — both thrones on one axis (task #292). Reading 1-2 Kings, the question is never who reigned but who reigned *at the same time*: the text dates every accession by the other kingdom's regnal year, so two lists side by side cannot answer it. Judah and Israel now share one vertical axis, selecting a king lights every king who stood on the other throne during his reign, and each king carries the accession synchronism plus both the Kings and the Chronicles account. **On Thiele throughout** — named in the header and carried in a `system` field so another chronology could be added without a migration — with every figure taken from a citable source recorded in the asset rather than derived arithmetically from the regnal totals, because accession-year convention and co-regencies are exactly what Thiele's system resolves. `scripts/build_hebrew_kings.py` asserts the invariants that make the table trustworthy: the throne never vacant between 931 and the two falls, reigns handing over at a shared year, no two sole reigns overlapping. **Co-regencies are modelled rather than flattened** — a king carries a *list* of spans, so Thiele's six Judean co-regencies render as the overlaps they are and Tibni, who never reigned alone, is representable at all. Reign lengths span four orders of magnitude, so bands stay true to scale and names live in a separate lane pushed apart by a pure, tested layout function. Deployed to dev; the release bump itself was left uncommitted by that run and is committed here.
- **v1.6.75** — **the prod "Null check operator used on a null value" was ours, and the error reporter had been eating every one of them.** Two reports on v1.6.65 the same day, iPhone/iOS-Safari and Mac/Chrome, identical message. **Fix the reporter first or you diagnose noise:** `runZonedGuarded`'s handler is typed `(Object, StackTrace)`, dart2js elides that in release, a zone error with a **null stack** got through, and `ErrorReporter.report` called `.toString()` on JS null — throwing *inside* the reporter, outside `_send`'s try/catch, so the real error was discarded and replaced by the reporter's own. Every zone error with a null stack had been arriving as this one string for months. `report()` now takes `Object?`/`StackTrace?` and survives an object whose own `toString()` throws. **The actual crash, deobfuscated without a rebuild:** the source map 200s as `<!DOCTYPE` (Netlify's SPA fallback again — the same trap as v1.6.64), so read the deployed bundle's line directly; `main.dart.js:49661` is a bare `n.toString`, dart2js's null-assert, sitting between an `await …go(-o)` and `uG(n.h(0,"state"),"flutter",…)` — i.e. `currentState! as Map` in `MultiEntriesBrowserHistory.tearDown`, engine `history.dart:225`. **Two conditions, both ours to break:** (a) `serialCount` lives in `history.state`, which is per-tab session history that iOS Safari keeps for months, so a count inherited from a *previous document* sends teardown's rewind into entries this document's engine never tagged; (b) `_writeStateToUrl` pushed `pushState(null, '', newHash)` on every chapter change, manufacturing exactly those untagged landing spots. Reproduced live (three null-state entries + `replaceState({serialCount:2})` + reload) — the reader's exact message, same line. **Not fatal, and that is the worst part:** the app keeps painting, but `_browserHistory` is left a *disposed* MultiEntries instance with its popstate listener gone, after which `serialCount` climbs on every navigation — the bug feeds itself across reloads (`{"serialCount":3,"state":null}` versus a healthy `{"flutter":true}`). So: `main()` zeroes an inherited count before the first frame, which is the one step that makes `if (backCount > 0)` false and skips the rewind; and our own entries carry `{'seeksparks': true}`. **Zeroed, not deleted** — `_hasSerialCount` tests the value not the key, so removing it reintroduces the crash by another door. **The marker is an *unrecognised* key on purpose,** not a copy of `{flutter: true}`: the engine's `onPopState` branches only on `origin == true` and `flutter == true`, so `null` and ours fall to the same `else` and back-button behaviour is unchanged by construction — then confirmed by running the same navigation script against both builds and getting identical hash, `history.length`, back, back and forward. **Deliberately not done:** migrating to `SystemNavigator.routeInformationUpdated`, because in single-entry mode `setRouteName` always *replaces* and Back would stop stepping chapters for everyone. **Still unproven:** what first put a count > 0 into that reader's tab (a tab opened on an older `Router` build, backgrounded and reloaded much later, fits; the phone path never writes a URL at all, so the small-screen advisory is not it). **`url_sync_service_web.dart` is behind a `dart.library.js_interop` import, so 1,879 green tests ran alongside this for months** — the decisions are now a pure function in `lib/utils/history_state_repair.dart` that a VM test drives, the untestable line is held by a source ratchet with an anti-vacuous length guard, and the *stub* is ratcheted too (a method missing there only fails on a native build, which CI never runs). Also fixed: `retired_version_notice.dart` used `firstWhere` with no `orElse` on a widget whose entire premise is that version codes outlive the versions they name — a retired fallback would have turned the notice into a `StateError` on frame one. **Found while measuring, NOT fixed: browser back/forward does not step through chapters on either build,** though `url_sync_service.dart`'s header says it does — the engine's single-entry `onPopState` `else` branch calls `go(-1)`, *removing* any entry it did not create, so it pops ours and snaps back. Long-standing, identical on prod; the honest fix is either URL-as-share-link-only or adopting a `Router` to earn multi-entry mode legitimately.
- **v1.6.76** — **three search defects a live audit found and 1,955 tests could not (#295).** `flutter test` has no DOM text field and no rendered results header, so all three shipped green; driving the deployed dev build over CDP — typing into the real command line, reading the real header — found them in an afternoon. **(1) The divine name reported its own frequency wrong.** `H3068` printed 共 500 節 because the header counted the refs it *had*, and the build pipeline truncates every entry's verse list at 500 — while the uncapped total sat loaded and discarded. 123 of 14,039 entries sit on that cap, hiding 160,564 occurrences; יהוה alone is 6,521. The header now reports **verses AND occurrences**, the two numbers BibleWorks' status line gives, which genuinely differ (G25 is 143 hits in 110 verses), and says outright when the list stopped early — suppressed under a search limit, where a Bible-wide total would read as the scope's own. **(2) The wildcard chip's own example did nothing.** Its tooltip advertises `faith✶`, tapping it after `faith` produces exactly that, and submitting searched for a *literal asterisk* — guaranteed empty, since no Bible text contains one. A bare `*` query now runs as `.query`. `?` is deliberately left alone: Scripture is full of question marks and `who is this?` is a substring search someone may have meant. **(3) `d nas` was refused** though the matcher's own doc comment opens with `nas` as its first example — it compared exact codes only. An unambiguous 3+ character prefix now resolves, exact still winning, so `kjv` stays KJV and never drifts to KJV+S and `l` stays the scope verb. **The third fix then had to be made twice**, which is the durable lesson: the display verbs resolved editions through `VerbContext.resolveVersion` in `command_verb.dart`, an independent exact-code-or-label comparison, so the command pane was fixed and the defect stayed on screen. One rule, two copies. `VerbContext` delegates to `matchVersionAbbreviation` now. **Both of these ran green the whole time** — when a claim is "the search box accepts X", the suite is not evidence.
- **v1.6.77** — **the Word Study tab stops being a bottom sheet parked in a pane (task #284).** The tab has **two bodies**: hovering a word gives `WordAnalysisPane`, hovering a verse or making a selection gives `OriginalsSheet` — and the second was still the phone's modal, so moving the pointer one grade coarser swapped the whole tab between two visual languages. **The header was worse than cosmetic:** `workbench_page.dart` skipped `WbPaneTitle` for this tab *precisely because* the sheet drew its own, so the **pane's** title and collapse chevron appeared and vanished as the pointer moved between a word and a verse. `WbPaneTitle` is unconditional now and the sheet draws no header; only the copy button survives, because it copies the word table this widget itself built (bwh10q puts the Forms tab's Options button in the body for the same reason). **The diff is far smaller than it looks, and knowing why is the reusable part:** `workbenchTheme` already remaps the *semantic* `ColorScheme` roles (`surface`, `onSurface`, `onSurfaceVariant`, `outline`, `primary`, `surfaceContainerHighest`), so **prose colour was never the problem**. What does not survive that remap is only three things — geometry, type scale, and the *tonal* roles (`primaryContainer`, `secondary`, `tertiaryContainer`) that `ColorScheme.fromSeed` fills with Material pastels the workbench palette has no equivalent for. Those three, and only those, are what `lib/constants/word_study_style.dart` resolves. **Both columns live in one pure data class rather than twenty-six inline `embedded ?` ternaries**, which is what makes "the phone modal did not move" an assertion instead of a hope — and a source ratchet can then demand that no literal `BorderRadius.circular(` survives in the widget (one exemption, marked `modal-only:`, for the distribution sheet, which is never docked). Docked: every radius zero, every border a hairline, Strong's numbers **green numerals instead of tinted pills**, and the type finally scaling with the workbench font-size setting — this was the one pane in the workbench that ignored it. **Two decisions worth inheriting.** The selected chip's ring is **`pinMark`, not a new colour**: tapping a chip here is the same act as pinning a word in Browse, and `pinMark` is the palette's one marker already contrast-checked (WCAG 3:1 non-text) against `selectionBg` in all three themes — including 護眼紙質, where the app's gold accent measures **1.54:1** and would simply vanish. And `chipMaxWidth` is **112** so two chips plus the gap and the list padding clear the 256px pane floor: 2×112 + 6 + 2×8 = 246. `listPadding` and the lemma size are copied from `WordAnalysisPane` deliberately — the tab's two bodies must not shift the left margin or resize the headword as the pointer crosses between them. **Verified by screenshot on dev, not assumed:** interlinear grid, tapped entry card, related-word chips, the CBOL attribution CC-BY-NC-SA 4.0 requires (now guarded by its own test, because losing it in a restyle is a licence breach), light + dark + 護眼紙質, and the pane title holding still across both bodies. Substance unchanged throughout.
- **v1.6.78** — **#279 reaches Bible Trivia, and the chrome was the smallest thing wrong with it (#279).** The brief said "restyle"; the page had **three defects the restyle would have preserved**. **(1) A curated entry pointed at a verse that does not exist:** `reference: 'Philemon 16'`, and because Philemon has one chapter `parseReference` read **16 as the chapter**, so that entry's jump landed nowhere — the one class of bug a 68-entry hand-written catalogue is guaranteed to have and no widget test was looking for. `trivia_catalogue_test.dart` now resolves **every** reference against `assets/kjv.json` (the asset stores `chapter`/`verse` as **strings** — `int.parse` them) and separately requires the five single-chapter books to carry an explicit `:`, because that failure mode is silent rather than throwing. **(2) The reference badge printed raw English** (`Genesis 1:1` under Chinese prose) — the #283 class on a surface #283 never listed; one call to `localizedReferenceLabel`, plus a test asserting the zh labels contain no `[A-Za-z]`. **(3) The testament toggle still said 旧约/新约**, and chasing it found the last two survivors in `stats_page.dart`; `statsBooksOT`/`statsBooksNT` are deleted, not merely unused. **The generalisable finding is about the test, not the page:** a page-level overflow smoke test under-tests everything behind a disclosure — the word-diagrams only build when a tile is *expanded*, so at 320 px they had never been rendered by anything; the new test drives the page's own search box and taps the tile rather than reaching for private widgets, which is also the only way to reach them from outside. Chrome itself was mostly **deleting page-level `shape:`/`backgroundColor:` overrides** — `workbench_theme` already squares `bottomSheetTheme` with `showDragHandle: false`, so both sheets were arguing with the theme; the hand-drawn rounded pill handle is **gone rather than squared**. **Conservative call, recorded as such:** bwh10k's Verse Tab is trivia's real BibleWorks analogue and would make this an auto-following Analysis tab, but the strip is at 10–11 tabs and #277/#284 already settled that another tab is not the answer — it stays a Resource. `ChapterVerseCountsDiagram` has **zero** instances in the catalogue and has therefore never rendered; left in place. `bible_trivia_page.dart` is on the `_passed` ratchet, one entry covering the page and both its sheets.
- **v1.6.80** — **#279 reaches the workbench's own centre pane, and the pass had been measuring the wrong population (#279).** The brief sized it as "13 of 25 pages in `lib/pages/`, 67 sites", so on that arithmetic it was half done — counting all of `lib/` finds **42 files / 130 sites**, and the **single heaviest offender in the app was `bible_reading_pane.dart` at 30 sites**, never on the list because it is not a page. That is the finding, not a detail: **by the governing principle the workbench IS the app, and the workbench is built of widgets** — `home_page.dart` has 2 sites and no route reaches it; the centre pane had 30 and is never not on screen. A **v1.6.50 handoff line was already wrong** because of this, recording that the rounded card and shadowed floating pill "belong to HomePage, a different route"; screenshots of dev show both sitting permanently above verse 1 and at the foot of the reading pane. **`_GlassSurface` — the auto-hide header, the bottom bar and the selection bar — gave three findings, only one of them chrome. (1) The blur never ran:** it took an `opaque` flag whose `false` branch built a `BackdropFilter`, and all three call sites passed `true`, so "glass" was a name for an opaque fill; the branch is deleted rather than kept as an option nothing selects. **(2) Squaring the corners is what makes the correct border legal** — `Border.paint` throws *"A borderRadius can only be given on borders with uniform colors"*, so the rounded version **could not** have a top-only hairline and had settled for a 4-sided outline whose bottom edge doubled the pane divider beneath it; it now takes `Border(top:)` or `Border(bottom:)` per anchor edge. The rule did not merely permit the better border, it was the precondition for it. **(3) Removing a shadow can force a colour change:** the progress pill's `surface` at 0.86 was legible only because the shadow lifted it off the scrolling verses, and one hairline cannot also hide bleed-through — it is opaque now. Generalises: **translucency and shadow are usually load-bearing together.** Also squared: `_BookIntroCard` (the literal card the spec forbids, the first thing above verse 1), `_SectionHeading`, `_FloatingHeader`, `_RefChip`, and every sheet the reader can open from the pane. **The ratchet gains the other half of its job:** `_passed` is a progress bar and cannot say how much is LEFT, which is exactly how "67 sites" was allowed to rot — `_remaining` now declares a per-file ceiling for every unconverted file in `lib/` and fails three ways (a clean file grows its first rounded corner, a dirty file grows more, a file reaches zero and is not promoted). Numbers may only go down, and lowering one makes the test demand the new figure. **Deliberately not done, and recorded as such:** bwh11 docks navigation in the Browse Window header and never overlays chrome on verse text, so the maximal reading of parity is to **dock these bars** — that is layout, layout is density, and density is out of #279's scope by the brief's own words; only corners, borders and shadows changed. **Two things the ratchet structurally cannot see.** Material gives `IconButton.filled`/`filledTonal` a `CircleBorder` by *default*, so the A−/A+ circles in the font-size sheet have no source line to grep — found only by screenshot, and the fix is one `iconButtonTheme` entry that lands on every surface at once, so it is its own iteration. And **a stale `main.dart.js` will lie convincingly**: the first capture of the local release build showed the rounded card still there against source that provably had none. Disable cache, re-navigate — and note that **navigating clears the CDP device-metrics override**, so re-issue the size step or every later clip lands in the wrong place. **Verified by screenshot at 1400 and 1012 px, light and dark:** header, intro panel, bottom bar, the wrapped two-row selection bar, the font-size sheet, and no overflow revealed by squaring. 護眼紙質 is reasoned, not shot — the `paperTheme` branch is a two-token swap inside the same square `DecoratedBox` verified in the other two themes.
- **v1.6.81** — **#279 reaches the book → chapter → verse picker, and the conversion was decided by hosting, not taste (#279).** After the centre pane, `book_chapter_picker.dart` was the heaviest file left (18 sites) and it is the app's **primary navigation surface** — the workbench Resources menu, the status bar's reference field and the reading pane all push it. **It has TWO hosts and only one is a page:** `BooksPage`, and `SidebarPanel` via `home_page.dart:325`. A widget with two hosts cannot rely on a host to theme it, so the picker now wraps **its own subtree** in `workbenchTheme` and `BooksPage` re-applies it for the AppBar and the ground the centred column sits on (idempotent — brightness comes from the parent, the paper flag from settings). **The failure this rules out is silent rather than loud:** `WbColors.of` falls back to `.light` when the extension is missing, so an unthemed host does not crash, it renders a light palette inside a dark app; two of the new tests exist only to pin that, mounting the picker in a deliberately unthemed host and asserting the palette resolved *inside* it. **Collapsing three implementations of "a number in a box" into one `_NumberTile` was not tidying:** `_chapterTile` drew a `Card`, `_gridChapterTile` a `Material` and `_VersePickerChip` a third thing, and the chapter grid was **the only one of the three missing `FontFeature.tabularFigures()`** — a column of 11/12/13 never lined up. `_bookTile` was declaring its fill and border **twice**, drawing a hairline over an already-bordered box. A duplicated widget hides a defect in two of its three copies. Deleting `BooksGlassSurface` also retires a **per-frame `BackdropFilter` over a scrolling list**, and its 0.78-alpha fill had to become opaque — the same v1.6.80 lesson, that translucency and shadow are load-bearing together. **The picker had no test at all** — only a four-width overflow smoke test that pumps it and asserts nothing — which is how a refactor that renders perfectly can navigate wrong: `_selectChapter` does **not** fire the caller's callback, it opens a verse grid; picking a verse sets `pendingJump` **before** firing so `books_page.dart:113` suppresses `jumpToTop`; "Top" fires with none, which is what makes jumpToTop legal again; and `didUpdateWidget` keeps the verse step across a same-book chapter change and drops it across books. **Two test traps worth inheriting, each of which cost a debugging round:** `AppSettings.booksViewMode` defaults to **`'grid'`**, so the surface an untouched reader sees prints the **abbreviation** (`Gen`), and a finder for `Genesis` reads as "the tile is not rendering"; and `AppSettings._locale` defaults to **`'zh-Hans'`**, so the first verse tile reads **本章开头**, not `Top`. The fix is not to hardcode the Chinese either — the test resolves `uiStrings['versePickerTop']![settings.locale]!` from the live settings object so it cannot drift. **Look user-visible strings up through the same table the widget uses.** **Conservative call, recorded as such:** bwh11 documents navigation **List Boxes** in the Browse Window header — version, book, chapter, verse as dropdowns, to "lookup various Bible references without doing any typing" — and SeekSparks already has them in the Browse frame; replacing this full-page picker with them is **layout**, which #279 scopes out. **Verified by screenshot on a local release build at 1440 and at 1000 px (the 992 px gate floor), light and dark:** book grid, chapter grid, verse grid, the expanding list view, and a `scale: 4` clip on the "Top" tile settling the corner at pixel level — square, one hairline, no shadow. The 800 px column cap finally reads as a panel because its gutters are hairlined instead of being two unexplained empty strips. 護眼紙質 and the `SidebarPanel` host are **reasoned, not shot** — the same one-line idiom as the pages already converted, and the two palette tests respectively.
- **v1.6.83** — **the 1,192-plate picture archive finally has a door, and the ratchet that was supposed to be guarding the chrome pass had a blind spot (#279, bwh07).** This run's feature was **already written**: the 15:55 watchdog kill took the previous iteration during *verification*, not mid-write, and the tree it left came back `analyze` exit 0 and **2065/2065** untouched — so the rule is now **test a killed run's dirty tree before discarding it**, because HANDOFF is written last and a killed run leaves no other record. What shipped: `assets/maps_index.json` has held 1,192 illustrations for months — 55 bundled maps and 1,137 public-domain Doré/Tissot/Schnorr plates, each with trilingual title and caption and a book/chapter filing across all 66 books — reachable **only** by already reading a chapter that happened to match, with `mapsBrowseLibrary` ("Browse all illustrations") sitting unused in `ui_strings` the whole time: the door was designed and never hung. bwh07 files a picture database under Resources, so that is where it went, beside the Atlas and the Family Tree, on the same consult-vs-operates test. **Where it beats the thing it is chasing:** Bible Views is photographs browsed *by place* with no idea which passage a picture belongs to; every plate here is chapter-scoped, so the index is ordered **canonically** and takes the #280 book scope — narrowed to Acts it stops being a bag of pictures and becomes Acts, in order, which is why `illustrationAnchor` is scope-aware rather than a fixed sort. Two defects came out with it: the viewer's filmstrip used to append **the entire library** under a header reading "Related illustrations", and `page_chrome_pass_test` matched `BorderRadius.circular(` only — blind to `BorderRadius.vertical(top: Radius.circular(16))`, the form *every bottom sheet uses*, which is how `bible_reading_pane` was recorded PASSED in v1.6.80 while carrying ten of them; ceilings that moved the day the matcher widened are annotated as **re-measurements, not regressions**. Thumbnails were correctness, not polish: 55 survey plates up to 1920×3288 are 95 MP, a grid decoding ~48 MP **never produced a frame**, and `cacheWidth` does not prevent the decode — 420 px copies are 2.4 MB total, regenerated by `tools/gen_map_thumbs.sh`, falling back to the full plate when absent. **Verified on dev by screenshot** at 1460 px in zh-Hant: the 插畫 entry in 資源, the grid in canonical Genesis order, kind chips summing to 1192, square corners and hairlines; the thumbnail asset was confirmed fetched in the network log, and the CDN plates confirmed **20/20 loaded** via `img.naturalWidth` — a stronger check than a frame grab, and necessary because full-page `Page.captureScreenshot` **hangs** on this grid while `eval` keeps answering (a clipped capture succeeds; the driver exits 0 printing nothing, which reads exactly like a broken step file). Book references follow the **reading version** per #283, so "Genesis 3" under a KJV reader in a Chinese UI is the rule, not the defect. **v1.6.82 has no entry above** — same killed run.
- **v1.6.87** — **the Analysis strip was measuring its labels in the wrong script, and the toggle the reader asked for (#297).** The reported symptom was 「…」: in a Chinese UI the labelled strip wrapped to two rows and then **cut every long label** — 原文…, 相关…, 经文…. The cause is one constant. `_kMinLabelledTabWidth = 66` decided whether a tab could carry a name, and its own comment described it as *"about six characters of 11.5 px text"* — **a Latin measurement applied to full-width glyphs**. 原文研读 is four of them, ~46 px of text before the icon, the 5 px gap and the padding, so the rule answered "the labels fit" and Flutter then ellipsised them. **English never showed it, Chinese always did**, and the suite cannot see it at all: `find.text` matches an ellipsised label by its full text, so a green run proves nothing here. The fix is to stop guessing — the width a labelled tab needs is now **measured with a `TextPainter` from the strings the strip will actually draw**, at the **selected tab's w700**, because measuring the lighter weight would leave the one tab the reader is looking at as the only one that gets cut. `analysisStripLayout` takes it as a parameter and stays pure and unit-tested; the label list is public for the same reason the measurement exists, so nothing can measure a copy that has drifted from what is drawn. **Two quieter faults came out with it.** The widget and the measurement now share one font size and one CJK fallback list, since a measurement of a different style is not a measurement; and **the three existing tests that asserted "labels come back when there is room" hard-coded that same 66** — under the test font, whose glyphs are squares, English needs ~147, so those tests were pumping a strip too narrow for its own labels and asserting about the wrong width. They derive it now. **The toggle** goes beside the pane's own collapse chevron — 「可不可以有toggle可以切换在这个menu旁边」 — with **two states, not three**: the strip already chooses well, so the only question worth a control is "decide for me" vs "I want the names", one tap either way. Automatic stays the default and still refuses more than two rows, because past two the strip eats the pane it labels. **Asked directly, it spends rows rather than letters** — down to one tab a row, which at any width this app can reach (256 px and up) is wider than any label, so the request is *always* honoured and the toggle can never look like the dead buttons of #294. **Verified by screenshot on dev in 繁體 at a ~264 px Analysis pane** — the wider of the two scripts: automatic draws two balanced rows of six icons; asked for names it draws four rows of three with **all twelve labels whole**, 原文研讀 / 相關經文 / 經文列表 included, no ellipsis anywhere; the button shows a pressed-in active state and the choice **survives a reload**. 简体 is covered by the widget tests (all twelve Simplified labels found whole at 420 px) — seeding `flutter.locale` did not switch the deployed app's UI language, which is a harness gap, not a product one. Also carried out on this deploy: **v1.6.86's #298 reader fix, which the watchdog killed mid-release** — dev had been sitting one commit behind since.
- **v1.6.88** — **the word list's romanisation described a different word than the row it sat on, and the same mistake was mirrored in the Word Study header (#303).** A word list row is a **lemma** — every inflection of ἔρχομαι counts into one row — but the row printed the **most frequent** surface form beside the **first occurrence's** transliteration, two different words on one line: John's G2064 row read `ἔρχεται · ēlthen`. Measured across all 66 books before the fix: **8,030 of 58,447 rows (13.7%)** printed a romanisation of a form other than the one shown, worst in Leviticus 24.6%, Exodus 23.1%, 1 Kings 20.9%, John 19.1%. The row is a lemma, so **all** of its romanisation now comes from the lexicon keyed by Strong's, and both defects vanish by construction. **The corpus only carries `t` for the Greek NT — 138,013 tokens have one, 300,808 Old Testament tokens have `t: null`** — so the Hebrew Bible was not showing a *wrong* romanisation, it was showing **none**; every OT row gains one for the first time. **There are two romanisation systems in this repo and they do not agree:** `assets/originals` uses plain macrons (`ēlthen`, `iēsou`), the Strong's lexicon uses the classic diacritics (`érchomai`, `Iēsoûs`, `ʼĕlôhîym`), and they are not convertible without inventing data — hence the rule now enforced by test: **a value describing a LEMMA comes from the lexicon, a value describing one OCCURRENCE comes from the corpus, and the two never share a column.** The **76 extended MorphGNT numbers** (G6000–G6095 with gaps, plus G6897/G6986/G7013/G7530/G9577/G9992) have no Strong's entry, and they get **no lemma and no romanisation at all** rather than the occurrence's — falling back there would restate the bug on exactly the rows nobody can check. **The audit found the same fault mirrored in `word_analysis_pane`**, which printed the occurrence form in red with the **lemma's** transliteration bracketed beside it (`ἦλθεν (érchomai)`); it now prints `lemma · translit` on its own line and **omits the lemma when it equals the form**, so `ὁ` reads just `ho`. Also fixed: the word-by-word **clipboard export** put `entry.translit ?? w.translit` in one column, mixing the two systems in a table a reader sorts in a spreadsheet; `context_words.dart` carried a first-occurrence value in a field **no widget ever read**, deleted rather than fixed. `StrongsService.lookupAll` exists because the precedent (`context_pane.dart`) awaited `lookup()` per row **and capped at 400 rows**, which would have silently dropped every lemma past the four-hundredth. **The trap that nearly hid a call site: a literal U+0000 byte in `lib/widgets/analysis_tabs.dart`** (a cache-key separator) made `grep -rn` print *"Binary file … matches"* with no lines and made **ripgrep skip the file silently** — a clean "no matches" for a file with four real hits, in an audit whose entire method was grep. It is written as a `\u` escape now; suspect this whenever a repo-wide grep comes back emptier than expected, and note that the same byte makes a shell heredoc fail outright — including the one that tried to write this sentence. The briefing's second figure (91.5% of 58,365) counts the ~39,500 rows with **no** occurrence romanisation as mismatches; of the 18,872 rows that had one, **13,924 (73.8%)** differed from the lemma's even after stripping diacritics — same data, different denominator, so **quote the denominator with the number**. **Verified by screenshot on dev in 繁體:** the Word List over John 1 showing `ἦν → G1510 · εἰμί · eimí`, `ἐρχόμενον → G2064 · ἔρχομαι · érchomai` (the reported row) and the collapse rule on `ὁ → G3588 · ho`; and the Word Study header over the LXX+WH line showing `ἀρχῇ` above `ἀρχή · archē` and `ὁ` above a bare `ho`. `flutter analyze` exit 0, **2,103 tests** exit 0, ten of them new and four of those loading the shipped `john.json` so the assertion is about the data that ships.

- **v1.6.89** — **the systematic data-integrity audit (#304), and the three editions that filed verses under labels nothing could resolve.** #303's premise was that 8,030 wrong rows surfaced only because one reader looked at one row; the question this run answers is **how many more has nobody looked at**. Method: cross-check assets that describe the same fact from different directions, so a disagreement is a bug in one of them. **Ten checks, 155k+ tagged keys, 438,821 tagged words, 294,820 verses, 279,597 cross-references, 9,856 place links** — full table in the new `docs/DATA-INTEGRITY.md`, counts reported **even where zero**, because "checked 31,102 references, found 0 gaps" is a different claim from "we believe it is fine". **Three defects found and repaired, all of them a LABEL, none a word of scripture.** (1) **`cuvs-plus` numbered the whole of 1 Chronicles 22 one verse low** — what every other edition calls 22:1 (「大卫说：这就是耶和华神的殿」) was filed as **21:31**, so ch21 held 31 verses and ch22 held 18. The nasty part: `assets/tagged/cuvs-plus/` carried the *same* shift, so the text and its Strong's tags agreed with each other and nothing looked odd — while `assets/originals` is canonical, so **the Word Study pane showed a 和简+ reader the Hebrew of the NEXT verse for the entire chapter**. Verified before touching it by normalising away the 雅伟/耶和华 restoration and the 藉/借 and 么/吗 orthography: cuvs-plus 21:31 is character-identical to cuvs-yhwh 22:1 and 22:N is 22:N+1 for all eighteen. (2) **`leb` labelled Micah "Mic" and Nahum "Nah"** (152 verses) — `Verse.id` is `bookNameToEnglish[book] ?? book`, so an LEB highlight in Micah was keyed `Mic-1-1` and never matched the same verse in any other edition, which is precisely the cross-version persistence that id exists to provide; ids also began `000`, book zero. (3) **`cuvs-yhwh-tr` labelled 2 Timothy 提摩太后書** (83 verses), simplified 后 in an otherwise traditional name, matching neither key in the table. **All three failed the new guards before the repair — checked by stashing the repaired assets and watching the test name all three, not by assuming.** Repairs are reproducible via `tools/repair_reference_defects.py`, and it preserves each file's own layout (leb/cuvs-yhwh-tr are indent-2, cuvs-plus and the tagged files are minified) — guessing wrong buried a 19-verse correction in a 217,000-line reflow on the first attempt. **THE WORST FINDING IS NOT FIXED, DELIBERATELY, AND IS THE NEXT SLICE: `assets/originals` is numbered in HEBREW versification and every display surface asks it in ENGLISH.** Found by asking fix (1)'s question of the whole corpus — all five tagged layers disagree with originals *identically* (Joel 21, Numbers 16, 1 Chronicles 15, 1 Kings 14, Job 8), and that uniformity is the tell: one systematic difference, not five import bugs. `OriginalsService` joins them by the **reader's** `'$chapter:$verse'` (`originals_service.dart:49`). Joel end to end: originals has four chapters (20/27/5/21, the Hebrew book), every edition has three (20/32/21); English **2:28** asks `originals['2:28']`, Hebrew ch2 ends at 27, pane is **empty**; English **3:1** asks `originals['3:1']` and is handed וְהָיָה אַחֲרֵי כֵן אֶשְׁפּוֹךְ — **the Hebrew of 2:28**. Not repaired because the fix is a sourced Hebrew↔English mapping and deriving it arithmetically is guessing at scripture. Also documented, not fixed: **`leb` is missing Judges (618 verses) and Obadiah (21) outright** — an import gap, and scripture is never invented to close one; 2,203 words carry no morphology code (the brief's prediction that codes were *undecodable* is **false** — all 4,037 distinct codes decode; the blanks are absent codes); and no date in `bible_timeline`/`hebrew_kings`/`family_tree` carries a per-record source. **Clean and worth saying so:** concordance.json's 14,039 entries all sum correctly **and agree exactly with `assets/originals`** (0 numbers in one and not the other, 0 totals disagreeing, 0 book-level cells disagreeing); all 9,856 `bible_places` links resolve with 0 unmapped abbreviations (Bethlehem is present); 0 Strong's numbers in the wrong testament; the 76 lexicon-less numbers are all extended-MorphGNT covering 86 words, enumerated in the test so a 77th cannot appear unnoticed. `flutter analyze` exit 0, **2,108 tests** exit 0, five of them new. **Verified on the deployed dev build by fetching the assets back**: `leb.json` serves Micah/Nahum and no longer serves Mic/Nah; `cuvs-plus.json` serves 1 Chronicles 21 with 30 verses and 22 with 19, and 22:1 is 「大卫说：这就是耶和华神的殿」.

- **v1.6.90** — **the reader's chapter-and-verse is not the original's, and for 1,823 references the Word Study pane answered with a different verse's Hebrew (#304, check 9).** `assets/originals` is numbered in the Masoretic tradition for the Hebrew Bible and the critical text for the Greek; every edition we ship is numbered in the English one. `OriginalsService` joined them on the reader's own `"chapter:verse"`, so where the traditions diverge the pane showed authoritative-looking Hebrew **about a verse the reader was not reading** — the worst kind of defect for a tool whose Greek nobody can check against anything except us. **The 152/204 the previous run reported was an undercount by an order of magnitude:** comparing key sets sees only verses missing from one side, while walking every reference found **1,823 resolving to another verse and 152 to none, across 36 books** — Psalms 979, 1 Chronicles 103, Deuteronomy 65, Daniel 62, Nehemiah 60, Exodus 58. **The table is derived, not asserted, because arithmetic on verse counts would be guessing at scripture.** `tools/derive_versification.py` reads the mapping out of the text instead: per book, a **banded monotone alignment** of the original's Strong's-number stream against three independent tagged translations (KJV+S, 和合本雅伟版, BSB), beads (1,1)/(1,2)/(2,1)/(1,3)/(1,0)/(0,1) scored by Dice over each verse's Strong's set, keeping a row **only where all three agree** — 110 rows failed unanimity and were dropped rather than guessed. Per-word voting was tried first and left 10% noise; it has no order constraint, which is the whole reason the monotone model works. **Three independent confirmations that it is TRUE and not merely self-consistent.** (1) It **reproduces the documented Masoretic table it was never given**: Gen 31:55→32:1, Lev 6:1→5:20, Num 16:36→17:1, 1 Kgs 4:21→5:1, 1 Chr 6:1→5:27, Job 41:1→40:25, Joel 2:28→3:1, Mal 4:1→3:19, 2 Cor 13:14→13:13, 3 John 14→14+15, and every psalm superscription including the four two-line ones (51, 52, 54, 60). (2) The **`absent` list fell out of the data** as exactly the **sixteen Received-Text verses** — Mt 17:21/18:11/23:14, Mk 7:16/9:44/9:46/11:26/15:28, Lk 17:36/23:17, Jn 5:4, Ac 8:37/15:34/24:7/28:29, Rom 16:24 — and no others; nothing in the pipeline knows that list, it emerges from KJV-vs-BSB disagreement because KJV follows the Received Text and BSB the critical one, so **their disagreements are evidence, not noise**. The Hebrew Bible adds exactly one, also unprompted: **Nehemiah 7:68**, the horses and mules, absent from BHS — a reader there was being shown the Hebrew for camels. (3) **A text-support gate on every changed row**: the new target must resemble the reader's own tagged verse at least as well as the identity it replaces. **1,971 improved, 19 equal, 0 degraded.** A deletion cannot be scored that way ("we now show nothing" scores 0 against everything, so the ordinary gate would always revert it and restore the wrong Hebrew) and is gated on the **rival claim** instead. **Fixed at the one join point, so all fifteen callers are corrected together**, and two directions are kept deliberately distinct: `originalKeys` is **coverage** (a reference returns every original verse it renders, so a merged psalm heading displays in full) while `rekeyBook` is a **partition** (each original verse lands under exactly one reference, so statistics and search cannot double-count) — **438,821 original words before the re-key and 438,821 after**. The pane now also distinguishes **"we have no data" from "there is nothing to have"**, a new localised string for the verses the critical edition omits. **Residue stated rather than papered over: Revelation 12:18 stays at identity** because KJV renders it inside 13:1 (Dice 0.973 vs 0.914) and BSB inside 12:17 (1.000 vs 0.927), so no single table serves both editions — under-coverage, not a false statement, which is the trade this document prefers. Ten more verses stayed at identity for want of unanimity (Ps 135:1-2, Zeph 2:1-2, Jn 7:53-8:1, Rev 12:17/13:1, Lk 1:1-2) and identity was checked to be right in every case; the disagreements are gaps in the Chinese tagging, not numbering differences. `flutter analyze` exit 0, **2,123 tests** exit 0, and the new suite **re-proves the map from the shipped assets** rather than pinning constants, so a regression in the data fails the run. **Verified by screenshot on deployed dev v1.6.90 in 繁體, both directions of the defect:** 約珥書 **2:28**, which returned nothing at all before, now shows H1961 וְהָיָה · H310 אַחֲרֵי · H8210 אֶשְׁפּוֹךְ and its WTT row in Browse — a row that simply did not exist when the lookup returned null — while 約珥書 **3:1**, which was handed 2:28's "I will pour out my spirit", now shows H3588 כִּי · H2009 הִנֵּה · H3117 בַּיָּמִים · H1992 הָהֵמָּה, which is Masoretic 4:1.

- **v1.6.91** — **the LEB shipped as a bad scrape: two books missing, and eight books ending with a word no Bible contains (#304).** `assets/leb.json` arrived at `6f13365 Initial commit` from the YsWords fork and had never been checked; the previous run documented its missing Judges and Obadiah as unfixable because **scripture is never invented to close a gap**. It is fixable with a *second copy of the same edition*, and chasing the two books turned up **five** defect classes with one signature — a scrape that lost page boundaries. **R1** Judges (618) and Obadiah (21) absent entirely, **639 verses restored**; **R2** two verses merged into one record so the second reference did not exist, **11 split**; **R3** a clause of the verse simply gone, **12 restored**; **R4** the **next book's title glued onto a book's last verse**, **8 trimmed**; **R5** three records filed under the book label `"The"`. **R2 and R4 are the ones that matter, because they read as scripture and are not**: `Hebrews 1:10` did not exist — 1:9 held both verses — so every cross-reference to it resolved to nothing or to the wrong verse, and `Romans 16:24` ended "…Amen. **Corinthians**" while `1 Peter 5:14` ended "…in Christ. **Peter**", so a reader quoting the verse quoted a word no edition of the Bible contains. **Nothing was invented, and the witness was measured before it was trusted for a single word**: over the 30,431 verses both copies hold they agree on **30,276 (99.491%)** whitespace-blind, which establishes edition identity, and converting the witness's HTML conventions into this repo's house style reproduces our own text **exactly in 30,151 (99.080%)** — that second figure is the **honest error bar on the imported material** and the residue is 125 Psalms structural differences plus ~155 verses where *our* copy is itself inconsistent about the space before a note marker. **Every repair is a containment check, so the script can only add text it has proven missing:** R3 asserts our text is a **prefix** of the witness's and appends only the tail; R4 asserts the trailing token equals the next book's name **and** that what remains ends in sentence-final punctuation (without that second clause it cuts into a sentence); R2 asserts our record **opens with** the first half and that what we hold is a **suffix** of the second. R2's guard began as strict equality and failed on all 11 — the diagnosis showed **one uniform cause** (our copy had also dropped the word introducing the quotation: `saying,` ×9, `and`, `And,`), and that uniformity is what licensed weakening it. **A hypothesis I killed by calibrating rather than assuming:** I believed the witness truncated block quotations and would corrupt poetic Judges 5, and built a word-count-ratio detector against BSB to catch it — the suspect verses scored 0.89–1.25 against a **30,169-verse median of 1.062**, and Judges/Obadiah a median 1.050, so **the witness is the correct copy and ours was the defective side**. The whole model inverted. **Also resolved the other way:** Romans 16:25-27, 2 Cor 13:14 and Acts 19:41 are **not** defects — the LEB carries the Romans doxology *inside* `<note: Some manuscripts include vv. 25-27, …>` on 16:24, so those verses genuinely do not exist in this edition. **After the repair LEB has 21 gaps and all 21 are known critical-text omissions** (Mt 17:21/18:11/23:14, Mk 7:16/9:44/9:46/11:26/15:28, Lk 17:36/23:17, Jn 5:4, Ac 8:37/15:34/19:41/24:7/28:29, Rom 16:25-27, 2 Cor 13:14, Neh 7:68) — enumerated one by one, nothing left over, which is a far stronger claim than "21 gaps remain". Repaired reproducibly by **`tools/repair_leb.py`** (idempotent; run with no flags to verify, `--write` to apply), and **matching the file's existing indent-2 shape is load-bearing** — a compact rewrite of the same data made the diff **213,865 deleted lines** instead of 4,581 added / 52 deleted, the second time that trap has cost a cycle. **Two standing guards generalise the classes across all 11 editions:** every edition must carry every book of the canon it covers (NT-only editions distinguished by whether they contain Genesis, not a hardcoded list — **this one would have caught the missing books on day one**), and no book may end with the name of the book that follows it (language-agnostic: strip a leading numeral from the next book's own label). Both pass everywhere now, plus a five-point regression pinning the repair. `flutter analyze` exit 0, **2,126 tests** exit 0, three suites new. **Known residue, stated rather than papered over: the witness drops the LEB's `{…}` idiom braces entirely** (0 of 31,102 records) whereas our 64 original books carry them in 8,041 verses (26%), so the 660 imported verses ship **without that notation** — under-coverage of a marker, not a false statement, the same trade as Rev 12:18. **Verified by fetching the asset back off deployed dev v1.6.91**: 31,199 records, 66 books, Judges 618, Obadiah 21, no `"The"`, `Hebrews 1:10` opens `And, "You, Lord`, `1 Peter 5:14` ends `who are in Christ.` **The wider lesson for the next reader: the code in this repo has been reviewed, the data it was seeded with never was.** Anything inherited from the fork should be assumed unchecked until a cross-check says otherwise.

- **v1.6.92** — **the Qere lives in a footnote, so 1,235 words of the Hebrew Bible had no parse at all (#304, check 2a).** 2,203 of 438,821 tagged words showed a blank parsing line in Word Study; the previous run recorded it as an upstream gap needing its own slice. **It was not upstream.** OSHB encodes the **Ketiv** as the verse's own `<w>` and the word actually **read aloud** one level down in a following sibling — `<note type="variant"><rdg type="x-qere"><w>` — so anything that iterates the verse's `<w>` children never sees the Qere. Two further shapes needed handling and are only reachable by walking `list(v)` in document order rather than by tag: **Qere without Ketiv** (read but not written, 10 cases, existing *only* inside the note) and **Ketiv without Qere** (a `<rdg>` with zero `<w>` children, 6 cases). Reading the Qere as authoritative and letting the Ketiv gap-fill takes the **Hebrew Bible from 1,252 untagged words to 1**. **That the old merge left those slots blank rather than borrowing the Ketiv's code is the only reason this was a coverage bug and not a wrongness bug:** measured, **653 of the 1,244 K/Q slots carry a DIFFERENT parse on the Qere than on the Ketiv**, so the obvious "fix" of borrowing would have printed a false parsing on over half of them with nothing looking odd. Blank was the honest failure, and the check that says so is worth more than the fix. Greek adds ~85 more via a **movable-nu** match (ἀπέχουσιν/ἀπέχουσι is one word with one parse, and SBLGNT may carry either), scoped to the environments where the nu is actually movable — `-σι`, `-ε`, `-τι` — and for a non-verb only where MorphGNT's own slots say **dative plural** (10-char code, case at index 6 = `D`, number at index 7 = `P`); ἐστί is why `-τι` is in that list and my first environment set was wrong without it. **A wider fallback was measured and REJECTED, and the rejection is the most important thing in this entry:** "if the untagged form appears elsewhere in the same verse with exactly one code, reuse it" closes **179** more Greek words and is **wrong** — SBLGNT's only τοῦ at Matthew 3:16 is the **neuter** of ἀπὸ τοῦ ὕδατος while the received text's second τοῦ is the **masculine** of τοῦ Θεοῦ, so the rule is plausible, prints a confident wrong gender, and no test in this repo could tell. It is written into `tools/merge_morphology.py`'s docstring so it is not rediscovered as a good idea. Caught only because I dumped both word sequences side by side **before** implementing. **Total 2,203 → 869 of 438,821 words (0.50% → 0.20%), 1,334 newly parsed. Rewriting all 66 asset files on the strength of "the number went down" is not evidence**, so the regeneration was fingerprinted before and after against what must NOT move: **0 words of text changed, 0 Strong's numbers changed, 0 transliterations changed, 0 parses lost**. It also surfaced the **2 parses that did change, both corrections** — 1 Chronicles 9:4 `בָּנִי` (H1137, the proper name Bani) had been parsed as a common-noun construct and is now `HNp`, and Proverbs 8:35 recovered a dropped 1cs suffix — neither of which is visible in a 440k-word diff without the fingerprint. **The 869 that remain are a fact about the editions, not a TODO**: the shipped Greek originals are a **received text** and SBLGNT is a **critical** one, so 868 readings have nothing to take a parse from, 222 of them being John 7:53–8:11 and Romans 16:25–27 entire — that exact set is now pinned by a test, so if it grows something regressed. The single Hebrew survivor is at **2 Samuel 18:20**. **Word Study now SAYS so instead of rendering nothing**: a missing parse prints a bordered line naming the corpus it was measured against (SBLGNT for Greek, Open Scriptures/WLC for Hebrew), because a blank that explains itself is information and a blank that does not is a bug report. **Corpus counts in tests are measurements and they move** — 1,334 new parses took `morph_search_service_test.dart` from 6,129 hits over 299,556 scanned to 6,192 over 300,807; rather than bumping the one failing number I re-derived **every** count in that file, confirmed the other three (Genesis qal-perfect 698, Matthew aorist-active-imperative 602, Aramaic Aphel 24) genuinely unchanged, and wrote the *reason* for the movement into the test. `flutter analyze` exit 0, **2,128 tests** exit 0, two new. **NEW DEFECT FOUND AND DELIBERATELY NOT FIXED, with the reasoning recorded:** four verses print the Ketiv **and** the Qere as two consecutive words — 2 Samuel 18:20 (`כי על על כן`, a doubled word to any reader), Jeremiah 51:3, Ezekiel 48:16, Proverbs 8:35. Measured the neighbouring phenomenon so the two are not conflated: the **1,257 unpointed Hebrew words** corpus-wide are the ordinary Ketiv convention and are correct. Deleting a word from shipped scripture is a text-editorial decision and the right repair is almost certainly a reader-side K/Q marker, so it went to `docs/DATA-INTEGRITY.md`'s queue rather than into this diff. **Verified on deployed dev v1.6.92, not assumed**: six `assets/originals` files fetched back off the deploy are byte-identical to the repo's, John serves 228 untagged of 15,635 words and Romans 89 of 7,111 while Genesis, 1 Chronicles and Proverbs serve **0**, 2 Samuel serves exactly **1** (the doubled `עַל` at 18:20), John 8:1 is wholly unparsed, and 1 Chronicles 9:4 Bani serves `HNp`. **Screenshot** of the new box on dev: clicking Ἰησοῦς in the BGT line of 約翰福音 8:1 pins a Word Study that now reads 「無詞形分析。希臘文詞形取自 SBLGNT，本處經文的部分讀法不見於該底本，故無可依據的分析。」 where it previously rendered nothing. The Hebrew wording of that box was **not** screenshotted — its only instance corpus-wide is 2 Samuel 18:20 and the driver could not reach that reference — so it was verified instead by finding all six new localised strings (zh-Hans/zh-Hant/en, both variants) in the deployed `main.dart.js`; the report says which claim rests on which. **The lesson for the next reader: when data is missing, check whether the plausible substitute is actually EQUAL before either congratulating the old code or replacing it** — here it was equal only 48% of the time.

- **v1.6.93** — **233 references printed the edition's typesetting instruction where the reader expected scripture (#304, check 14).** 和合本 combines verses under one number and prints 「見上節」 in the number column of the ones it swallowed; the Septuagint file stores the literal string `OMIT` for verses the critical text lacks. Both are *instructions to a typesetter*, and the app was rendering them as the verse's text — so 詩篇 8:7 read「见上节」in the reader, `copyVerseText` pasted it into a sermon as though it were the psalm, and a search for 上節 returned **227 hits that are not scripture**. **The previous audit saw this and deferred it as "presentation, not accuracy"; that reasoning was wrong and the doc now says so.** A string the reader will quote is a claim about the text regardless of why it is in the file. **The census is the deliverable, not the fix** — 295,527 records walked, every non-scripture reference classified and counted: **213 merged** (71 each in cuvs-yhwh / cuvs-yhwh-tr / cuvs-plus, four stored spellings including the long form 「合和譯本並入上一節」), **3 forward-merge** 「见下节」, **16 `OMIT`** in lxxwh, **1 blank** in biblexg-v2-tr. **The head is not `verse - 1`, and the data proves it rather than the comment asserting it:** merges CHAIN — 詩篇 8:7 **and** 8:8 are both 「见上节」 and both belong to verse **6**, so the obvious resolver would have sent the reader from one placeholder to another. `mergedVerseHeads` walks the chapter forward and remembers the last reference that had words; a test pins the Psalm 8 shape verbatim, and asserts that an `OMIT` or a blank can never *become* a head. **Whole-match, never `contains`** — 「除酵节，又名逾越节，近了。」 contains 上节 and is real scripture, so a substring rule would have blanked the verse it was meant to protect; the recogniser matches the trimmed string exactly (plus the `<note:…>` wrapper the editions use), with a 24-character length gate so it never scans a real verse. **The note is localised to the UI locale, not the edition's script** — a 繁體 reader on a 简体 Bible gets 「與第 29 節合併印行」, because the sentence is the app speaking about the edition, not the edition speaking. **Where the logic lives is the point:** the whole rule is a pure file, `lib/utils/verse_text_absence.dart`, with no Flutter import — the reader, Browse, the clipboard and the search-key builders all ask it the same question, which is what made the missing surface findable. **One was missing:** `verse_popup_sheet.dart`, reached from sermon-citation chips, both rendered and *copied* the raw marker; grepping every caller of `parseScripture|sanitizeVerseText` is how it turned up, and it is fixed in the same commit. KWIC and the concordance are covered because they read the tagged layer, which placeholders have no entry in — that is **reasoning, not measurement**, and the doc labels it as such. **The corpus test is deliberately wider than the fix, because a census alone is circular:** if an edition adopted a new phrasing tomorrow the count would just be updated. A second test greps every short verse mentioning the verse above or below (`上[节節]|下[节節]|并入|並入|OMIT`) and fails on **any** that the recogniser does not classify — 0 today, so drift surfaces in CI instead of on the reader's screen. A third asserts all 213 merged references resolve to a head that actually has words. `flutter analyze` exit **0**, **2,148 tests** exit 0 (+20). **Verified by screenshot on deployed dev v1.6.93, not assumed:** 創世紀 24:30 in the 雅繁+ row now reads **「與第 29 節合併印行」** in muted italic — the correct head, the correct script for the 繁體 UI, and no 見上節 anywhere — while the BSB and LXX+WH rows for the same reference show their own real text, which is the per-edition behaviour the fix requires. **Not verified:** the `OMIT` note and the English strings were exercised only by unit test. **The lesson: a placeholder is data about the book, and the app must never let it pass for the book.**

- **v1.6.95** — **the search-statistics chart drew the 500-verse cap and called it the word (#308).** The report was a question about a label — 「the 27 in John, is it the number of actual word frequency in John or the number of 'verses'?」 — and the label was indeed missing: `Most in: John 27` never said what 27 counted, while the same word occurs 37 times in John, so a reader arriving from software that plots occurrences was wrong by ten. bwh23 makes it an explicit menu choice ("Number of verses in the book containing a hit" vs "Number of hits in the book") for exactly this reason. **Measuring it found something worse under it.** `concordance.json` describes each word twice — an uncapped per-book map of OCCURRENCES (`b`, sums exactly to the total) and a VERSE list (`r`) that the build pipeline stops at **500 entries in canonical order** — and the strip tallied the second one. **123 of 14,039 entries reach that cap, and 58 of them then name the wrong heaviest book.** H3068 (יהוה) drew as **3 books peaking in Exodus 341**; the truth is **36 books peaking in Jeremiah 712**. The header directly above already said 「僅列出前 500 節」, so the app admitted the list was cut and then charted the cut as if it were the shape of the divine name. **The fix is not a label, it is a choice of source:** the unit is now a declared property (`HitUnit`) that every builder must name and every surface prints, and `strongsDistribution` draws **whichever description is a CENSUS** — verses while the list is whole, the uncapped occurrence map once it is not, and, where neither is complete, **no chart at all** plus a sentence saying why. Two cases hit that last branch: a composed expression (`G3588 AND G2532` is set algebra over two prefixes that both end inside Matthew — the result is 335 verses and every one of them is in Matthew), and a scoped result over a capped entry, where the whole-Bible map describes a different corpus than the listed verses do and so can be neither the primary count nor the secondary one. Where both counts are known the strip prints **both** (`約翰福音 27 (37×)`), because the pair settles the distinction permanently; only the leading book carries it, since three pairs will not fit a 240 px pane, and the tooltip cannot be relied on to carry it instead — **a tablet has no hover** (#299). **It also closes the unit mix #290 documented and deferred:** the Stats tab printed `inScope / total` as capped VERSES over uncapped OCCURRENCES. A whole-book limit is now answered by summing the occurrence map over those books (same unit, no cap); a chapter-level limit can only be answered from the verse list, so it says so in its own hint string, drops the denominator rather than inventing a comparable one, and prints **`≥N`** when the list it read was cut — a floor is true where a count would not be. `flutter analyze` exit **0**, **2,174 tests** exit 0 (+13, including a regression that freezes both H3068 shapes so the Exodus/3-books answer can never come back). **Verified by screenshot on deployed dev v1.6.95, not assumed** — all four cases in the 繁體 UI at 1400 and at 1000 (just above the 992 gate): H3068 → 「最多出現於（按出現次數）：耶利米書 712 · 詩篇 687 · 申命記 548 · 36 卷書」; G25 → 「共 110 節 · 143 處」 with 「最多出現於（按節）：約翰福音 27 (37×) · 約翰一書 17 · 路加福音 9 · 22 卷書」, every figure matching the asset exactly; the composed expression → no bars and the truncation sentence; and no overflow or ellipsis at the narrow width. **Not verified by screenshot:** the Stats-tab scoped hint and the `≥N` floor, which were exercised only by unit test and by reading the data. **Found and NOT fixed, deliberately** (recorded in `docs/DATA-INTEGRITY.md`): a *scoped search* over a capped entry silently returns wrong RESULTS, not just a wrong chart — `l jer` + `H3068` filters the 500-ref prefix, which ends in Leviticus, and yields **zero** hits where Jeremiah holds 712 occurrences. That needs `concordance.json` regenerated from `assets/originals/` against an asset-size budget, which is its own item. **The lesson: when a surface and the data disagree about what was counted, do not label the surface — find out which of the two sources is a census, and refuse to draw when neither is.**

- **v1.6.96** — **a scoped search read a 500-verse prefix as the whole word, and answered zero (#304, check 15).** `concordance.json`'s `r` stopped at 500 refs per Strong's number, and `r` is in **canonical order** — so a capped entry was never a 500-verse sample, it was Genesis-through-wherever-500-landed (H3068's ended at Leviticus 2:14). Anything that *filters* or *intersects* the list inherited that silently, because a filter over a prefix returns a well-formed empty answer: `l jer` then `H3068` returned **zero verses for the divine name in Jeremiah**, where it occurs 712 times, with no qualification. 123 of 14,039 entries reached the cap. #308 found this the day before and recorded it as "blocked on an asset-size decision" — but the decision was only ever blocked on a **measurement**: uncapping costs 1.75 MB raw, **307 KB gzipped**, and there was no trade-off, just an unmeasured number standing in for one. A compact ordinal/delta re-encoding would have been cheaper and was rejected as the *less* conservative option, since it needs a canon-ordinal table between the generator and every consumer and this repo has already shipped one versification defect from that class (check 9). The regenerated asset was proved a **pure superset before shipping** — 13,916 entries identical, 123 strict extensions, 0 refs dropped or moved, `n` and `b` untouched — so the fix cannot have changed an answer that was already right. **The trap worth carrying forward: removing a limitation obliges you to audit everything built to apologise for it.** #308's truncation apparatus inferred "cut" from a list that *reached* the cap, so left in place it would have printed 「首 500 節」 above a complete 5,522-verse list — the fix manufacturing its own untruth on exactly the words it had just repaired, with all 2,180 tests green, because the tests pinned the machinery rather than the claim. The signal is repointed at the one real remaining incompleteness (a **wildcard** expansion stopped at its 300-number limit; measured: 13 prefixes exceed it), and the Stats tab's `≥` prefix is gone. Five tests freeze it, two naming no number and both failing on the old data: **the books in `r` must equal the keys of `b`** (121 entries fail under the cap) and **no long list length may be shared by more than 20 entries** (a cap is invisible per-record, obvious in the distribution — 123 words all "happening" to occur exactly 500 times). `ConcordanceService.lookup` is now memoised; it regex-parses every ref and KWIC re-looks-up per page, which at 14× the list length is 6,977 matches per "load more". Verified on dev, not assumed: `l jer` + `H3068` now answers **共 614 節**, and unscoped **共 5,522 節 · 6,521 處** with the chart drawn across the whole canon (最多出現於（按節）：Psalms 627 (687×) · Jeremiah 614) — the two paths agreeing on Jeremiah's 614 independently.

- **v1.6.97** — **three sermons stopped a third of the way through in Traditional Chinese, and ten more are summaries wearing the clothes of a translation (#310).** `assets/sermons/` — ~45 MB of preaching the app presents exactly the way it presents scripture — had never been audited by #304. Two defects, found as two ratios. **First:** Han characters in each zh-TW body over its zh-CN source is **1.000 in the median and never below 0.9949**, except 100 (**0.353**), 369 (**0.425**) and 370 (**0.736**) — no gradient in between, so not a style. They do not read as broken: each tracks the Simplified for two paragraphs and then condenses to the end, so sermon 100 finishes while still setting up the parable it is named for. The defect is **upstream, not in this repo's ingest** — in the source corpus every `<id>.zh-TW.txt` is byte-identical to `<id>.zh-proofread.txt`, and 370 proves it at part level (part a abridged 8,427 Han vs 22,507, part b intact 30,807 vs 30,695, which is exactly why it kept 73%). **The conversion was derived, not assumed**, because the wrong OpenCC config would have quietly rewritten three files into a variant system the other 286 do not use: the shipped Traditional corpus uses 爲 **23,701** times against 為 635, 裏 10,262 against 裡 311, 着 6,915 against 著 375 — all the `s2t` forms, all the ones `s2tw`/`s2twp` flip — and `s2t(zh-CN)` reproduces **192 of 286** healthy files byte-for-byte and **99.866%** of characters overall, while the reverse holds for only 100, which is what fixes the direction. An earlier positional character diff produced a plausible "second conversion stage" (爲→為 ×328, "→「 ×297); it was an alignment artefact and **was overturned by measurement before anything was written** — re-run through `difflib.SequenceMatcher` the table dissolved into human proofreading, including real content edits (`'access'`→「進入的途徑」) that a regeneration must not invent. Index titles left alone: `titles['zh-TW']` is s2t of `titles['zh-CN']` in 285 of 289, the four exceptions are benign (「vs.」 set as 「對比」), and the body H1 agrees with the index in only 153 — so re-deriving titles would have churned correct data on worse evidence. **Second, and larger, found only by asking the other axis:** Chinese characters per English word is **1.403** in the median, and **ten files sit at 0.097–0.194 with nothing at all between them and 0.933**. Those ten zh-CN bodies are not translations, they are competent summaries — a tenth of the length, ordinary paragraphs, a proper closing line, sermon 126 opening 「讓我們先回顧這個診斷性比喻的核心信息」, i.e. prose written *about* the sermon. Nine of the ten are one topic batch ("The Parables of Jesus"); their Traditional bodies are faithful conversions **of the summary**, so both Chinese locales are affected. **This one cannot be repaired here and pretending otherwise would be the defect** — no full Chinese exists upstream, and writing ~85,000 words is translation, not a fix. So the conservative option, named as such: `index.json` marks them `"condensed"`, the page prints a hairline notice above the body naming the text a summary and pointing at the language that holds the whole sermon, and **the notice is prepended to the clipboard copy**, because a summary pasted into someone's notes without it is indistinguishable from the preaching and will be quoted as the preaching. Both thresholds sit in empty bands rather than in a gradient, and **both tests were proven by restoring the defective data and watching them fail** (85% of the Simplified; and the marked set must equal the re-measured set, so an unearned mark fails as loudly as an unmarked summary). `flutter analyze` exit **0**, **2,190 tests** exit 0 (+9). Verified by screenshot on deployed dev v1.6.97, not assumed: the Traditional body of 126 shows 「本篇為內容摘要，並非講道全文。完整講道見英文。」, the English body of the same sermon shows **nothing** (it is the complete one), and the Simplified body under an **English** UI shows "This text is a condensed summary, not the full sermon. The full text is available in English." — the sentence following the interface language rather than the body's, on one line, no overflow. Deployed assets re-fetched over HTTP and byte-matched to local, with all ten markers live. **Carried forward for the owner, not an engineering task:** the ten sermons need a real Chinese translation; until then the marking is the honest state. **The lesson: when a repair would mean generating the content, mark and disclose instead — and when a brief names one defect, measure the other axis before believing it is the only one.**
